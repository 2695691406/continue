---
name: leader-tools
description: Leader Agent的完整工具定义，包含编排调度、任务管理和子agent管理等专用工具
agent: leader
tool_count: 29
---

<functions>

<!-- ============================================ -->
<!-- Leader 特有工具：编排调度、任务管理、子agent管理 -->
<!-- ============================================ -->

<function>
{"name": "TaskCreate", "description": "Create a new task in the task system.\n\nWhen to use:\n- Complex multi-step tasks (more than 3 independent steps).\n- Work tracking for non-trivial tasks.\n- When the user provides a list of multiple tasks.\n- When using Plan Mode.\n\nWhen NOT to use:\n- Single simple tasks.\n- Tasks completable within 3 simple steps.\n- Pure conversation or information queries.\n\nTask Field Specifications:\n- subject: Short imperative sentence (e.g. 'Fix authentication bug in login flow'). No step numbers.\n- activeForm: Present continuous tense (e.g. 'Fixing authentication bug'). Displayed when in_progress.\n- All tasks are created with pending status.", "parameters": {"type": "object", "properties": {"subject": {"type": "string", "description": "Task title in imperative form (e.g. 'Fix authentication bug in login flow')."}, "description": {"type": "string", "description": "Detailed description of the task including objectives, context, and acceptance criteria."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress (e.g. 'Fixing authentication bug')."}, "metadata": {"type": "object", "description": "Optional metadata keys to associate with the task."}}, "required": ["subject", "description"]}}
</function>

<function>
{"name": "TaskList", "description": "List all tasks in the task system.\n\nReturns a summary of all tasks with their IDs, subjects, statuses, and dependencies.\n\nUse this to:\n- Get an overview of all current tasks.\n- Check which tasks are pending, in_progress, or completed.\n- Identify unblocked tasks ready for dispatch.\n- Find your next task after completing one.", "parameters": {"type": "object", "properties": {}}}
</function>

<function>
{"name": "Agent", "description": "Launch a subagent to execute a specific task.\n\nUse this tool to dispatch work to specialized expert roles:\n- coding-expert: Full-stack code implementation\n- research-expert: Research and analysis, codebase investigation\n- code-review-expert: Code review, finding bugs and vulnerabilities\n- verify-expert: Verification and testing (tests, lint, type checking, builds)\n- browser-expert: Browser interaction, end-to-end UI verification\n- researcher: Technical research, technology selection, feasibility assessment\n- frontend-dev: Frontend development (UI components, pages, styling)\n- backend-dev: Backend development (API, database, server-side)\n\nIMPORTANT:\n- Provide complete context so the agent can work independently.\n- Include task objective, scope boundaries, necessary context, acceptance criteria, and output requirements.\n- The subagent_type must be one of the valid expert role identifiers.", "parameters": {"type": "object", "properties": {"prompt": {"type": "string", "description": "The complete task description and context for the subagent. Must include: task objective, scope boundaries, necessary context, acceptance criteria, and output requirements."}, "subagent_type": {"type": "string", "description": "The type/role of the subagent to launch (e.g. 'coding-expert', 'research-expert', 'verify-expert', 'browser-expert', 'code-review-expert', 'researcher', 'frontend-dev', 'backend-dev')."}, "name": {"type": "string", "description": "Optional custom name for the subagent instance."}, "description": {"type": "string", "description": "Optional brief description of the subagent's purpose."}, "role": {"type": "string", "description": "Optional role override for the subagent."}, "task_ids": {"type": "array", "items": {"type": "string"}, "description": "Optional task IDs to associate with this subagent."}, "resume": {"type": "boolean", "description": "Optional. If true, resume an existing subagent instead of creating a new one."}}, "required": ["prompt", "subagent_type"]}}
</function>

<function>
{"name": "switch_mode", "description": "Switch the current interaction mode.\n\nUse this tool to switch to Plan Mode when:\n- The task involves architecture design, technology selection, or system design choices.\n- The task affects multiple files and modules, requiring large-scale changes.\n- Requirements are unclear, with multiple valid implementation approaches and significant trade-offs.\n- Multi-step complex tasks with dependencies between steps.\n- The user explicitly requests planning first.\n\nDo NOT switch to Plan Mode when:\n- The task has a simple, clear objective.\n- Already in Plan Mode.", "parameters": {"type": "object", "properties": {"target_mode_id": {"type": "string", "description": "The ID of the target mode to switch to (e.g. 'plan' for Plan Mode)."}}, "required": ["target_mode_id"]}}
</function>

<function>
{"name": "create_plan", "description": "Create or update an execution plan for complex tasks.\n\nUse this tool within Plan Mode to formulate comprehensive plans that include:\n- Task decomposition with clear dependencies.\n- Execution order and parallelization strategy.\n- Acceptance criteria for each subtask.\n- Risk assessment and mitigation.\n\nModes:\n- 'write': Create or overwrite the plan.\n- 'notify_update': Notify that the plan has been updated.", "parameters": {"type": "object", "properties": {"mode": {"type": "string", "description": "The plan operation mode. Must be 'write' to create/overwrite a plan, or 'notify_update' to notify of plan updates."}, "name": {"type": "string", "description": "The name/title of the plan."}, "overview": {"type": "string", "description": "A brief overview/summary of the plan."}, "plan": {"type": "string", "description": "The full plan content in markdown format. Includes task decomposition, dependencies, execution order, and acceptance criteria."}}, "required": ["mode"]}}
</function>

<function>
{"name": "ask_user_question", "description": "Ask the user one or more clarification questions.\n\nUse this tool when:\n- Requirements are ambiguous and could be interpreted multiple ways.\n- Critical information is missing that would affect implementation.\n- There are significant trade-offs that the user should decide on.\n- Before dispatching work that might be wasted without clarification.\n\nBest practices:\n- Ask targeted, specific questions.\n- Provide options when applicable to make it easy for the user to respond.\n- Batch related questions together.", "parameters": {"type": "object", "properties": {"questions": {"type": "array", "items": {"type": "object", "properties": {"question": {"type": "string", "description": "The question text to ask the user."}, "options": {"type": "array", "items": {"type": "string"}, "description": "Optional list of predefined answer options for the user to choose from."}}}, "description": "A question object with the question text and optional answer options."}, "description": "Array of question objects to ask the user."}}, "required": ["questions"]}}
</function>

<function>
{"name": "Skill", "description": "Execute a skill within the main conversation.\n\nWhen users ask you to perform tasks, check if any of the available skills match. Skills provide specialized capabilities and domain knowledge.\n\nWhen users reference a 'slash command' or '/<something>' (e.g. '/commit', '/review-pr'), they are referring to a skill. Use this tool to invoke it.\n\nHow to invoke:\n- Use this tool with the skill name and optional arguments.\n- Examples:\n  - skill: 'pdf' - invoke the pdf skill\n  - skill: 'commit', args: '-m Fix bug' - invoke with arguments\n  - skill: 'review-pr', args: '123' - invoke with arguments\n\nIMPORTANT:\n- Available skills are listed in system-reminder messages in the conversation.\n- When a skill matches the user's request, invoke it BEFORE generating any other response.\n- NEVER mention a skill without actually calling this tool.\n- Do not invoke a skill that is already running.", "parameters": {"type": "object", "properties": {"skill": {"type": "string", "description": "The skill name (e.g. 'commit', 'review-pr', 'pdf')."}, "args": {"type": "string", "description": "Optional arguments for the skill."}}, "required": ["skill"]}}
</function>

<function>
{"name": "fetch_rules", "description": "Fetch detailed rule content by rule names.\n\nUse this tool to retrieve the full content of specific rules when you need to understand or apply project-specific rules and guidelines.\n\nThe rule names are typically discovered from the project configuration or referenced in other project files.", "parameters": {"type": "object", "properties": {"rule_names": {"type": "array", "items": {"type": "string"}, "description": "The name of a rule to fetch."}, "description": "Array of rule names to fetch detailed content for."}}, "required": ["rule_names"]}}
</function>

<!-- ============================================ -->
<!-- 通用工具：与子agent共享的工具定义               -->
<!-- ============================================ -->

<function>
{"name": "read_file", "description": "Reads a file from the local filesystem. You can access any file directly by using this tool.\n\nUsage:\n- Provide the absolute path to the target file.\n- You can read the entire file by providing just the file path, which is recommended for better understanding and accuracy.\n- For very large files or when you need specific sections, you can optionally specify line ranges using start_line and end_line.\n- Supports reading image files (jpeg/jpg, png, webp).\n- If the file does not exist, an error will be returned.\n\nBest Practices:\n- Always ensure you have the COMPLETE context before making decisions.\n- If the retrieved range is insufficient, call this tool again to fetch additional content.\n- Use line ranges only when the file is too large to read at once.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the file to read."}, "start_line": {"type": "integer", "description": "The line number to start reading from, 1-based. Only provide if the file is too large to read at once."}, "end_line": {"type": "integer", "description": "The inclusive line number to end reading at, 1-based. Only provide if the file is too large to read at once."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "search_replace", "description": "Performs exact string replacements in files.\n\nUsage:\n- When editing text, ensure you preserve the exact indentation (tabs/spaces) as it appears before.\n- ALWAYS prefer editing existing files in the codebase. NEVER write new files unless explicitly required.\n- The edit will FAIL if old_string is not unique in the file. Either provide a larger string with more surrounding context to make it unique, or use replace_all to change every instance of old_string.\n- Use replace_all for replacing and renaming strings across the file. This parameter is useful if you want to rename a variable, for instance.\n- To create or overwrite a file, you should prefer the create_file tool.\n\nIMPORTANT:\n- old_string and new_string must be different.\n- Only use emojis if the user explicitly requests it.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The path to the file to modify. You can use either a relative path in the workspace or an absolute path."}, "old_string": {"type": "string", "description": "The text to replace. Must be an exact match of existing content in the file."}, "new_string": {"type": "string", "description": "The text to replace it with. Must be different from old_string."}, "replace_all": {"type": "boolean", "description": "If true, replace all occurrences of old_string in the file. Default is false."}}, "required": ["file_path", "old_string", "new_string"]}}
</function>

<function>
{"name": "create_file", "description": "Use this tool to create a new file with content or modify existing files by overwriting.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): Absolute path to the target file.\n- file_content (REQUIRED): The content of the file.\n- add_last_line_newline (OPTIONAL): Whether to add a newline at the end (default: true).\n\nIMPORTANT:\n- MUST LIMIT THE FILE CONTENT TO AT MOST 1000 LINES.\n- When editing existing files, MUST use search_replace tool FIRST, EXCEPT for COMPLETE file modification or overwrite.\n- ALWAYS prefer editing existing files in the codebase.\n- NEVER write new files unless explicitly required.\n- NEVER proactively create documentation files (*.md) or README files unless explicitly requested.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the target file. Must be generated first before any other arguments."}, "file_content": {"type": "string", "description": "The content of the file."}, "add_last_line_newline": {"type": "boolean", "description": "Whether to add a newline at the end of the file. Default is true."}}, "required": ["file_path", "file_content"]}}
</function>

<function>
{"name": "delete_file", "description": "Delete a file safely. ONLY use this tool to delete files.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): The absolute path to the file to delete.\n\nIMPORTANT:\n- This is the only safe tool to delete files.\n- DO NOT use shell commands or launch-process to remove files.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The absolute path to the file to delete."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "grep_code", "description": "High-performance file content search utilizing the ripgrep engine.\n\nGuidelines:\n- Recommended for precise string matching. Faster than terminal grep/rg and honors .gitignore automatically.\n- Full regex support available, such as \"error.*Handler\", \"class\\\\s+\\\\w+\". Special characters require escaping for exact matches, e.g. \"getUser\\\\(\".\n- Pattern format follows ripgrep conventions (not standard grep). Literal braces require escaping (e.g. map\\\\[string\\\\] to match map[string] in Go).\n- Broad glob patterns like '--glob *' should be avoided as they circumvent .gitignore and reduce performance.\n- Apply 'type' or 'glob' parameters only when file type is certain.\n- Results auto-expand to complete syntax blocks (functions, methods, structs) for better context.\n- Line matching: Single-line by default. Cross-line patterns require multiline: true flag.", "parameters": {"type": "object", "properties": {"regex": {"type": "string", "description": "Regex pattern for searching within file contents (rg --regexp)."}, "path": {"type": "string", "description": "Target file or directory for search (rg PATTERN [PATH ...]). Uses workspace roots by default."}, "type": {"type": "string", "description": "Specific file type for search (rg --type). Common options: js, py, rust, go, java, etc. Preferred over glob for standard types."}, "glob": {"type": "string", "description": "File filtering glob pattern (rg --glob GLOB -- PATH), such as \"*.java\" or \"*.{js,ts,tsx}\"."}, "multiline": {"type": "boolean", "description": "Activate multiline matching where . includes newlines and patterns cross line boundaries (rg -U --multiline-dotall). Default: false."}, "-A": {"type": "number", "description": "Lines of context to display after match (rg -A)."}, "-B": {"type": "number", "description": "Lines of context to display before match (rg -B)."}, "-C": {"type": "number", "description": "Lines of context to display both before and after match (rg -C)."}, "-i": {"type": "boolean", "description": "Perform case-insensitive search (rg -i). Default: false."}}, "required": ["regex"]}}
</function>

<function>
{"name": "search_file", "description": "Search for files by glob pattern (such as *.go or config/*.json) in workspace.\nONLY supports glob patterns, NOT regex. This only returns the paths of matching files. Limited to 25 results.\nMake your query more specific if you need to filter results further.\n\nExamples:\n- \"*.go\" (converted to \"**/*.go\") - matches all .go files\n- \"**/src/**\" - matches all files under src directories\n- \"**/test/**/test_*.py\" - matches test_*.py files within any test directory\n\nIf the pattern does not begin with \"**/\", it will be automatically prefixed with \"**/\" for recursive search.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Glob pattern for matching files."}, "path": {"type": "string", "description": "Directory path to search within. Supports both absolute paths and relative paths (resolved from workspace root). When omitted, searches from the workspace root."}}, "required": ["query"]}}
</function>

<function>
{"name": "list_dir", "description": "List the contents of a directory. Useful to understand the file structure before diving deeper into specific files.\n\nIMPORTANT:\n- The path parameter must be an absolute path.\n- Unless requested by the user, do not recursively check directories layer by layer; try to lock the directory location first before viewing.", "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Absolute path to the directory to list contents of."}}, "required": ["path"]}}
</function>

<function>
{"name": "run_in_terminal", "description": "Execute a shell command in the terminal.\n\nIMPORTANT: This tool is for terminal operations like git, npm, docker, etc. DO NOT use it for file operations (reading, writing, editing, searching, finding files) - use the specialized tools instead.\n\nRules:\n1. Ensure commands are compatible with the user's operating system.\n2. Working directory inherits from previous step if shell reuse is possible; otherwise uses workspace root. Use absolute paths and avoid cd.\n3. For long-running commands or those expected to keep running, set is_background to true. Use get_terminal_output to check background process output later.\n4. For commands requiring user interaction, pass non-interactive flags (e.g. --yes).\n5. Do not delete directories or files through commands.\n6. Do not use commands that may harm the user's OS (e.g. rm, sudo, wget, curl).\n7. Do not use newline characters in the command.\n8. Commands must not exceed 500 characters. Write longer commands to a file first.\n9. Assess risk before execution. Read-only commands are generally safe; destructive commands (rm, wget) are risky.\n10. NEVER update git config or run destructive/irreversible git commands unless explicitly requested.\n11. NEVER commit changes unless explicitly asked.", "parameters": {"type": "object", "properties": {"command": {"type": "string", "description": "REQUIRED. The command to run in the terminal."}, "is_background": {"type": "boolean", "description": "REQUIRED. Whether the command starts a background process. If true, runs in background and returns a terminal ID for later output checking."}, "timeout": {"type": "integer", "description": "Optional. Timeout in milliseconds for command completion (default 180000, max 1800000). Foreground commands exceeding timeout are auto-converted to background."}, "has_risk": {"type": "boolean", "description": "Whether the command is potentially risky (e.g. destructive operations)."}, "required_permissions": {"type": "string", "description": "Optional. Set to 'all' to request running outside sandbox. Only use when confirmed failure was caused by sandbox restrictions."}, "command_names": {"type": "array", "items": {"type": "string"}, "description": "Optional. Parsed command names from the command (e.g. [\"cd\", \"ls\"])."}}, "required": ["command", "is_background"]}}
</function>

<function>
{"name": "get_terminal_output", "description": "Get the output of a terminal command previously started with run_in_terminal. The tool will intelligently wait for command completion based on the wait_seconds parameter.\n\nTips:\n- Choose wait_seconds based on command type: 2s for quick commands (ls, cat), 15s for compilation/tests, 30s for application startup.\n- Default wait is 2s.", "parameters": {"type": "object", "properties": {"terminal_id": {"type": "string", "description": "The ID of the terminal command output to check."}, "wait_seconds": {"type": "integer", "description": "Wait time in seconds for command completion. Default is 2."}}, "required": ["terminal_id"]}}
</function>

<function>
{"name": "search_codebase", "description": "Semantic code search that finds code by meaning, not exact text. Use when you don't know which files contain needed info, or to ask high-level 'where/how' questions about the codebase.\n\nExamples: \"authentication logic\", \"how payments work\", \"database connection handling\".\n\nDecision Rule: If query contains PascalCase, camelCase, or 'class/interface/method + Name', use search_symbol instead.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "A concise, keyword-rich search string optimized for code retrieval. Should contain technical terms, relevant programming concepts, and implementation-focused language. Emphasize code functionality, architecture components, and technical specifications."}, "key_words": {"type": "string", "description": "Extract maximum three most important keywords from the query, ranked by relevance. Prioritize keywords most relevant to the query's core intent. Ignore generic programming words. Format: keyword1,keyword2."}, "target_directories": {"type": "array", "items": {"type": "string"}, "description": "Absolute directory paths to narrow the search scope. Each path must be a workspace root or subdirectory. Glob patterns are not supported."}}, "required": ["query", "key_words"]}}
</function>

<function>
{"name": "search_symbol", "description": "Tool for discovering code symbols and their relationships.\n\nWHEN TO USE:\n- For class names (e.g. \"PmsProduct\")\n- For method names (e.g. \"UserService.createUser\", \"createUser\")\n- For any visible symbol names in queries/messages\n- Discover related symbols through relationships\n\nUsage:\n- Accepts array of search objects via \"queries\".\n- Each query needs \"symbol\" (exact name to search).\n- Optional: \"file_path\", \"relation\".\n\nRelation filter options:\n- calls: Symbols called by the current symbol\n- called_by: Symbols that call the current symbol\n- references: Symbols referenced by the current symbol\n- referenced_by: Symbols that reference the current symbol\n- extends: Symbols extended by the current symbol\n- extended_by: Symbols that extend the current symbol\n- implements: Interfaces implemented by the current symbol\n- implemented_by: Symbols that implement the current symbol\n- contains: Symbols contained within the current symbol\n- contained_by: Symbols that contain the current symbol\n- overrides: Methods overridden by the current symbol\n- overridden_by: Symbols that override the current symbol\n- all: All relationships\n- none: Only symbol definitions, no relationships\n\nIMPORTANT: Avoid using 'all' relation when possible. Empty result means no matches found.", "parameters": {"type": "object", "properties": {"queries": {"type": "array", "items": {"type": "object", "properties": {"symbol": {"type": "string", "description": "The symbol name to search for. One symbol per object."}, "relation": {"type": "string", "enum": ["calls", "called_by", "references", "referenced_by", "extends", "extended_by", "implements", "implemented_by", "contains", "contained_by", "overrides", "overridden_by", "all", "none"], "description": "The relationship to retrieve for this symbol."}, "file_path": {"type": "string", "description": "Optional file path to narrow the search."}}}}, "description": "Array of search objects, each describing a single symbol lookup."}, "required": ["queries"]}}
</function>

<function>
{"name": "get_problems", "description": "Get any compile or lint errors in a code file. If the user mentions errors or problems in a file, they may be referring to these. Use the tool to see the same errors that the user is seeing.", "parameters": {"type": "object", "properties": {"file_paths": {"type": "array", "items": {"type": "string", "description": "The absolute path of a file."}, "description": "The absolute paths of mentioned or edited files. Multiple files supported."}}, "required": ["file_paths"]}}
</function>

<function>
{"name": "lsp", "description": "Interact with the Language Server Protocol to get code intelligence features.\n\nCapabilities include:\n- Go to definition: Navigate to where a symbol is defined.\n- Find references: Find all usages of a symbol across the codebase.\n- Get hover information: Retrieve type information and documentation for a symbol.\n- Get diagnostics: Get compiler/linter errors and warnings for a file.\n\nUse this tool for precise, language-aware code navigation and analysis that goes beyond text-based search.", "parameters": {"type": "object", "properties": {"action": {"type": "string", "description": "The LSP action to perform (e.g. 'definition', 'references', 'hover', 'diagnostics')."}, "file_path": {"type": "string", "description": "The absolute path to the file."}, "line": {"type": "integer", "description": "The 0-based line number of the target symbol."}, "character": {"type": "integer", "description": "The 0-based character offset of the target symbol on the line."}}, "required": ["action", "file_path"]}}
</function>

<function>
{"name": "fetch_content", "description": "Fetches the main content from a web page. The URL must be an HTTP or HTTPS URL pointing to a valid internet resource accessible via web browser.\n\nUseful for summarizing or analyzing the content of a webpage. Use this tool when looking for information from a specific webpage.", "parameters": {"type": "object", "properties": {"url": {"type": "string", "description": "URL to read content from. Must start with http:// or https://."}, "query": {"type": "string", "description": "The query to search for in the web page's content. Should be a clear and concise description of the content you want to find."}}, "required": ["url"]}}
</function>

<function>
{"name": "search_web", "description": "Explore the web for real-time information on any topic.\n\nUse this tool when you need up-to-date information that might not be included in existing knowledge, or when you need to verify current facts. The search results will include relevant snippets and URLs from web pages.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Search query. Value range: 1 to 100 characters."}, "timeRange": {"type": "string", "enum": ["OneDay", "OneWeek", "OneMonth", "OneYear", "NoLimit"], "description": "The time range of the query. Default is NoLimit."}}, "required": ["query"]}}
</function>

<function>
{"name": "TaskGet", "description": "Retrieve a task by its ID from the task list.\n\nWhen to use:\n- When you need the full description and context before starting work on a task.\n- To understand task dependencies (what it blocks, what blocks it).\n- After being assigned a task, to get complete requirements.\n\nOutput returns full task details: subject, description, status ('pending', 'in_progress', or 'completed'), blocks, and blockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to retrieve."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "TaskUpdate", "description": "Update a task in the task list.\n\nWhen to use:\n- Mark tasks as resolved when work is completed.\n- Update task details when requirements change.\n- Establish dependencies between tasks.\n\nIMPORTANT:\n- Always mark assigned tasks as resolved when finished.\n- ONLY mark a task as completed when FULLY accomplished.\n- If encountering errors or blockers, keep as in_progress.\n- Never mark as completed if tests are failing, implementation is partial, or unresolved errors exist.\n\nStatus progresses: pending → in_progress → completed.\n\nUpdatable fields: status, subject, description, activeForm, owner, metadata, addBlocks, addBlockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to update."}, "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled", "failed"], "description": "New status for the task."}, "subject": {"type": "string", "description": "New subject/title for the task (imperative form)."}, "description": {"type": "string", "description": "New description for the task."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress (e.g. 'Running tests')."}, "owner": {"type": "string", "description": "New owner for the task (agent name)."}, "metadata": {"type": "object", "description": "Metadata keys to merge into the task. Set a key to null to delete it."}, "addBlocks": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that this task blocks."}, "addBlockedBy": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that block this task."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "SendMessage", "description": "Send a message to another agent in the team.\n\nUse this tool to communicate with other agents (leader or experts). Messages are stored in the recipient's inbox and can be polled for new messages.\n\nCommon use cases:\n- Notify the leader about task completion.\n- Ask questions to other agents.\n- Share important findings or results.\n- Request help or clarification.\n\nThe recipient will receive the message when they check their inbox.", "parameters": {"type": "object", "properties": {"recipient": {"type": "string", "description": "The name of the recipient agent (e.g. 'leader', expert agent name)."}, "content": {"type": "string", "description": "The main content of the message."}, "summary": {"type": "string", "description": "A brief summary of the message (shown in notifications)."}, "type": {"type": "string", "enum": ["message"], "description": "The type of message. Use 'message' for standard messages."}}, "required": ["recipient", "content"]}}
</function>

<function>
{"name": "todo_write", "description": "Use this tool to create and manage a todo list for tracking complex multi-step tasks.\n\nUse proactively when:\n1. Complex multi-step tasks requiring 3+ distinct steps.\n2. Non-trivial tasks requiring careful planning.\n3. User explicitly requests a todo list.\n4. User provides multiple tasks.\n5. After receiving new instructions to capture requirements.\n\nSkip when:\n1. Single, straightforward task.\n2. Trivial task with no tracking benefit.\n3. Task completable in fewer than 3 trivial steps.\n4. Purely conversational or informational task.\n\nStatus values:\n- PENDING: Not yet started.\n- IN_PROGRESS: Currently working on.\n- COMPLETE: Finished all required outputs.\n- CANCELLED: No longer relevant.", "parameters": {"type": "object", "properties": {"todos": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "description": "Unique identifier for the todo item."}, "content": {"type": "string", "description": "The description/content of the todo item (max 70 characters recommended)."}, "status": {"type": "string", "enum": ["PENDING", "IN_PROGRESS", "COMPLETE", "CANCELLED"], "description": "The status of the todo item."}}}, "description": "Array of todo items (minimum 2 items recommended)."}, "merge": {"type": "boolean", "description": "Whether to merge the todos with existing. true=merge by id, false=replace all."}}, "required": ["merge", "todos"]}}
</function>

<function>
{"name": "update_memory", "description": "Update, add, or delete memories to maintain memory consistency.\n\nWHEN TO USE:\n- When the user explicitly requests it.\n- When the user's intent is to add, delete, or modify a memory.\n\nBefore performing the operation, if the relevant memory does not already exist, use search_memory first to check the current memory status.\n\nMemory categories include:\n- User Preferences: user_info, user_hobby, user_communication\n- Project Information: project_tech_stack, project_build_configuration, project_dependency_configuration, project_ide_configuration, project_scm_configuration, project_environment_configuration, project_introduction\n- Development Standards: development_code_specification, development_practice_specification, development_test_specification, development_comment_specification\n- Lessons Learned: expert_experience, learned_skill_experience", "parameters": {"type": "object", "properties": {"title": {"type": "string", "description": "Descriptive title for a new or updated memory. Required when creating or updating."}, "content": {"type": "string", "description": "Content of a new or updated memory. Leave blank when deleting."}, "keywords": {"type": "string", "description": "Keywords to associate with the memory, comma-separated. Used for filtering and retrieval. Only used when creating."}, "category": {"type": "string", "description": "Memory category. Must be one of the predefined categories."}, "action": {"type": "string", "description": "The type of action: 'create', 'update', or 'delete'."}, "id": {"type": "string", "description": "ID of an existing memory to update or delete. Leave blank when creating."}, "scope": {"type": "string", "description": "Set to 'global' (cross-project) or 'workspace' (current project only). Only set when user explicitly requests."}}, "required": ["title", "content", "keywords", "category", "action"]}}
</function>

<function>
{"name": "search_memory", "description": "Retrieve detailed memories from the memory system.\n\nWHEN TO USE:\n- At any task node to recall relevant memories for efficient resolution.\n- At task start, retrieve background information and past experiences.\n- When using other retrieval tools, recall more relevant details.\n- If a tool execution fails, recall potential solutions.\n\nSearch approach:\n- First perform a shallow search for relevant keywords and categories.\n- If task is not completed after shallow search, perform a deep search.\n- When user explicitly requests recall or expresses doubt, use deep mode.\n\nDepth levels:\n- 'shallow': Smaller search scope, fewer but highly relevant memories. Use when task information is sufficient.\n- 'deep': Larger search scope, more comprehensive memories. Use when more information is needed or memory overview shows few relevant memories.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "What you want to recall. Keep it semantically clear, complete, and concise."}, "keywords": {"type": "string", "description": "Keywords describing the memory. Up to 5 keywords, each max 10 characters, comma-separated."}, "category": {"type": "string", "description": "Categories to search. Comma-separated. Use empty string to search all categories."}, "depth": {"type": "string", "description": "Recall depth: 'shallow' for focused results, 'deep' for comprehensive retrieval."}}, "required": ["query", "keywords", "category", "depth"]}}
</function>

</functions>


---
name: frontend-dev-tools
description: frontend-dev角色的完整工具定义
agent: frontend-dev
tool_count: 20
---

<functions>

<function>
{"name": "read_file", "description": "Reads a file from the local filesystem. You can access any file directly by using this tool.\n\nUsage:\n- Provide the absolute path to the target file.\n- You can read the entire file by providing just the file path, which is recommended for better understanding and accuracy.\n- For very large files or when you need specific sections, you can optionally specify line ranges using start_line and end_line.\n- Supports reading image files (jpeg/jpg, png, webp).\n- If the file does not exist, an error will be returned.\n\nBest Practices:\n- Always ensure you have the COMPLETE context before making decisions.\n- If the retrieved range is insufficient, call this tool again to fetch additional content.\n- Use line ranges only when the file is too large to read at once.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the file to read."}, "start_line": {"type": "integer", "description": "The line number to start reading from, 1-based. Only provide if the file is too large to read at once."}, "end_line": {"type": "integer", "description": "The inclusive line number to end reading at, 1-based. Only provide if the file is too large to read at once."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "search_replace", "description": "Performs exact string replacements in files.\n\nUsage:\n- When editing text, ensure you preserve the exact indentation (tabs/spaces) as it appears before.\n- ALWAYS prefer editing existing files in the codebase. NEVER write new files unless explicitly required.\n- The edit will FAIL if old_string is not unique in the file. Either provide a larger string with more surrounding context to make it unique, or use replace_all to change every instance of old_string.\n- Use replace_all for replacing and renaming strings across the file. This parameter is useful if you want to rename a variable, for instance.\n- To create or overwrite a file, you should prefer the create_file tool.\n\nIMPORTANT:\n- old_string and new_string must be different.\n- Only use emojis if the user explicitly requests it.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The path to the file to modify. You can use either a relative path in the workspace or an absolute path."}, "old_string": {"type": "string", "description": "The text to replace. Must be an exact match of existing content in the file."}, "new_string": {"type": "string", "description": "The text to replace it with. Must be different from old_string."}, "replace_all": {"type": "boolean", "description": "If true, replace all occurrences of old_string in the file. Default is false."}}, "required": ["file_path", "old_string", "new_string"]}}
</function>

<function>
{"name": "create_file", "description": "Use this tool to create a new file with content or modify existing files by overwriting.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): Absolute path to the target file.\n- file_content (REQUIRED): The content of the file.\n- add_last_line_newline (OPTIONAL): Whether to add a newline at the end (default: true).\n\nIMPORTANT:\n- MUST LIMIT THE FILE CONTENT TO AT MOST 1000 LINES.\n- When editing existing files, MUST use search_replace tool FIRST, EXCEPT for COMPLETE file modification or overwrite.\n- ALWAYS prefer editing existing files in the codebase.\n- NEVER write new files unless explicitly required.\n- NEVER proactively create documentation files (*.md) or README files unless explicitly requested.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the target file. Must be generated first before any other arguments."}, "file_content": {"type": "string", "description": "The content of the file."}, "add_last_line_newline": {"type": "boolean", "description": "Whether to add a newline at the end of the file. Default is true."}}, "required": ["file_path", "file_content"]}}
</function>

<function>
{"name": "delete_file", "description": "Delete a file safely. ONLY use this tool to delete files.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): The absolute path to the file to delete.\n\nIMPORTANT:\n- This is the only safe tool to delete files.\n- DO NOT use shell commands or launch-process to remove files.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The absolute path to the file to delete."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "grep_code", "description": "High-performance file content search utilizing the ripgrep engine.\n\nGuidelines:\n- Recommended for precise string matching. Faster than terminal grep/rg and honors .gitignore automatically.\n- Full regex support available, such as \"error.*Handler\", \"class\\\\s+\\\\w+\". Special characters require escaping for exact matches, e.g. \"getUser\\\\(\".\n- Pattern format follows ripgrep conventions (not standard grep). Literal braces require escaping (e.g. map\\\\[string\\\\] to match map[string] in Go).\n- Broad glob patterns like '--glob *' should be avoided as they circumvent .gitignore and reduce performance.\n- Apply 'type' or 'glob' parameters only when file type is certain.\n- Results auto-expand to complete syntax blocks (functions, methods, structs) for better context.\n- Line matching: Single-line by default. Cross-line patterns require multiline: true flag.", "parameters": {"type": "object", "properties": {"regex": {"type": "string", "description": "Regex pattern for searching within file contents (rg --regexp)."}, "path": {"type": "string", "description": "Target file or directory for search (rg PATTERN [PATH ...]). Uses workspace roots by default."}, "type": {"type": "string", "description": "Specific file type for search (rg --type). Common options: js, py, rust, go, java, etc. Preferred over glob for standard types."}, "glob": {"type": "string", "description": "File filtering glob pattern (rg --glob GLOB -- PATH), such as \"*.java\" or \"*.{js,ts,tsx}\"."}, "multiline": {"type": "boolean", "description": "Activate multiline matching where . includes newlines and patterns cross line boundaries (rg -U --multiline-dotall). Default: false."}, "-A": {"type": "number", "description": "Lines of context to display after match (rg -A)."}, "-B": {"type": "number", "description": "Lines of context to display before match (rg -B)."}, "-C": {"type": "number", "description": "Lines of context to display both before and after match (rg -C)."}, "-i": {"type": "boolean", "description": "Perform case-insensitive search (rg -i). Default: false."}}, "required": ["regex"]}}
</function>

<function>
{"name": "search_file", "description": "Search for files by glob pattern (such as *.go or config/*.json) in workspace.\nONLY supports glob patterns, NOT regex. This only returns the paths of matching files. Limited to 25 results.\nMake your query more specific if you need to filter results further.\n\nExamples:\n- \"*.go\" (converted to \"**/*.go\") - matches all .go files\n- \"**/src/**\" - matches all files under src directories\n- \"**/test/**/test_*.py\" - matches test_*.py files within any test directory\n\nIf the pattern does not begin with \"**/\", it will be automatically prefixed with \"**/\" for recursive search.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Glob pattern for matching files."}, "path": {"type": "string", "description": "Directory path to search within. Supports both absolute paths and relative paths (resolved from workspace root). When omitted, searches from the workspace root."}}, "required": ["query"]}}
</function>

<function>
{"name": "list_dir", "description": "List the contents of a directory. Useful to understand the file structure before diving deeper into specific files.\n\nIMPORTANT:\n- The path parameter must be an absolute path.\n- Unless requested by the user, do not recursively check directories layer by layer; try to lock the directory location first before viewing.", "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Absolute path to the directory to list contents of."}}, "required": ["path"]}}
</function>

<function>
{"name": "run_in_terminal", "description": "Execute a shell command in the terminal.\n\nIMPORTANT: This tool is for terminal operations like git, npm, docker, etc. DO NOT use it for file operations (reading, writing, editing, searching, finding files) - use the specialized tools instead.\n\nRules:\n1. Ensure commands are compatible with the user's operating system.\n2. Working directory inherits from previous step if shell reuse is possible; otherwise uses workspace root. Use absolute paths and avoid cd.\n3. For long-running commands or those expected to keep running, set is_background to true. Use get_terminal_output to check background process output later.\n4. For commands requiring user interaction, pass non-interactive flags (e.g. --yes).\n5. Do not delete directories or files through commands.\n6. Do not use commands that may harm the user's OS (e.g. rm, sudo, wget, curl).\n7. Do not use newline characters in the command.\n8. Commands must not exceed 500 characters. Write longer commands to a file first.\n9. Assess risk before execution. Read-only commands are generally safe; destructive commands (rm, wget) are risky.\n10. NEVER update git config or run destructive/irreversible git commands unless explicitly requested.\n11. NEVER commit changes unless explicitly asked.", "parameters": {"type": "object", "properties": {"command": {"type": "string", "description": "REQUIRED. The command to run in the terminal."}, "is_background": {"type": "boolean", "description": "REQUIRED. Whether the command starts a background process. If true, runs in background and returns a terminal ID for later output checking."}, "timeout": {"type": "integer", "description": "Optional. Timeout in milliseconds for command completion (default 180000, max 1800000). Foreground commands exceeding timeout are auto-converted to background."}, "has_risk": {"type": "boolean", "description": "Whether the command is potentially risky (e.g. destructive operations)."}, "required_permissions": {"type": "string", "description": "Optional. Set to 'all' to request running outside sandbox. Only use when confirmed failure was caused by sandbox restrictions."}, "command_names": {"type": "array", "items": {"type": "string"}, "description": "Optional. Parsed command names from the command (e.g. [\"cd\", \"ls\"])."}}, "required": ["command", "is_background"]}}
</function>

<function>
{"name": "get_terminal_output", "description": "Get the output of a terminal command previously started with run_in_terminal. The tool will intelligently wait for command completion based on the wait_seconds parameter.\n\nTips:\n- Choose wait_seconds based on command type: 2s for quick commands (ls, cat), 15s for compilation/tests, 30s for application startup.\n- Default wait is 2s.", "parameters": {"type": "object", "properties": {"terminal_id": {"type": "string", "description": "The ID of the terminal command output to check."}, "wait_seconds": {"type": "integer", "description": "Wait time in seconds for command completion. Default is 2."}}, "required": ["terminal_id"]}}
</function>

<function>
{"name": "search_codebase", "description": "Semantic code search that finds code by meaning, not exact text. Use when you don't know which files contain needed info, or to ask high-level 'where/how' questions about the codebase.\n\nExamples: \"authentication logic\", \"how payments work\", \"database connection handling\".\n\nDecision Rule: If query contains PascalCase, camelCase, or 'class/interface/method + Name', use search_symbol instead.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "A concise, keyword-rich search string optimized for code retrieval. Should contain technical terms, relevant programming concepts, and implementation-focused language. Emphasize code functionality, architecture components, and technical specifications."}, "key_words": {"type": "string", "description": "Extract maximum three most important keywords from the query, ranked by relevance. Prioritize keywords most relevant to the query's core intent. Ignore generic programming words. Format: keyword1,keyword2."}, "target_directories": {"type": "array", "items": {"type": "string"}, "description": "Absolute directory paths to narrow the search scope. Each path must be a workspace root or subdirectory. Glob patterns are not supported."}}, "required": ["query", "key_words"]}}
</function>

<function>
{"name": "search_symbol", "description": "Tool for discovering code symbols and their relationships.\n\nWHEN TO USE:\n- For class names (e.g. \"PmsProduct\")\n- For method names (e.g. \"UserService.createUser\", \"createUser\")\n- For any visible symbol names in queries/messages\n- Discover related symbols through relationships\n\nUsage:\n- Accepts array of search objects via \"queries\".\n- Each query needs \"symbol\" (exact name to search).\n- Optional: \"file_path\", \"relation\".\n\nRelation filter options:\n- calls: Symbols called by the current symbol\n- called_by: Symbols that call the current symbol\n- references: Symbols referenced by the current symbol\n- referenced_by: Symbols that reference the current symbol\n- extends: Symbols extended by the current symbol\n- extended_by: Symbols that extend the current symbol\n- implements: Interfaces implemented by the current symbol\n- implemented_by: Symbols that implement the current symbol\n- contains: Symbols contained within the current symbol\n- contained_by: Symbols that contain the current symbol\n- overrides: Methods overridden by the current symbol\n- overridden_by: Symbols that override the current symbol\n- all: All relationships\n- none: Only symbol definitions, no relationships\n\nIMPORTANT: Avoid using 'all' relation when possible. Empty result means no matches found.", "parameters": {"type": "object", "properties": {"queries": {"type": "array", "items": {"type": "object", "properties": {"symbol": {"type": "string", "description": "The symbol name to search for. One symbol per object."}, "relation": {"type": "string", "enum": ["calls", "called_by", "references", "referenced_by", "extends", "extended_by", "implements", "implemented_by", "contains", "contained_by", "overrides", "overridden_by", "all", "none"], "description": "The relationship to retrieve for this symbol."}, "file_path": {"type": "string", "description": "Optional file path to narrow the search."}}}}, "description": "Array of search objects, each describing a single symbol lookup."}, "required": ["queries"]}}
</function>

<function>
{"name": "fetch_content", "description": "Fetches the main content from a web page. The URL must be an HTTP or HTTPS URL pointing to a valid internet resource accessible via web browser.\n\nUseful for summarizing or analyzing the content of a webpage. Use this tool when looking for information from a specific webpage.", "parameters": {"type": "object", "properties": {"url": {"type": "string", "description": "URL to read content from. Must start with http:// or https://."}, "query": {"type": "string", "description": "The query to search for in the web page's content. Should be a clear and concise description of the content you want to find."}}, "required": ["url"]}}
</function>

<function>
{"name": "search_web", "description": "Explore the web for real-time information on any topic.\n\nUse this tool when you need up-to-date information that might not be included in existing knowledge, or when you need to verify current facts. The search results will include relevant snippets and URLs from web pages.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Search query. Value range: 1 to 100 characters."}, "timeRange": {"type": "string", "enum": ["OneDay", "OneWeek", "OneMonth", "OneYear", "NoLimit"], "description": "The time range of the query. Default is NoLimit."}}, "required": ["query"]}}
</function>

<function>
{"name": "get_problems", "description": "Get any compile or lint errors in a code file. If the user mentions errors or problems in a file, they may be referring to these. Use the tool to see the same errors that the user is seeing.", "parameters": {"type": "object", "properties": {"file_paths": {"type": "array", "items": {"type": "string", "description": "The absolute path of a file."}, "description": "The absolute paths of mentioned or edited files. Multiple files supported."}}, "required": ["file_paths"]}}
</function>

<function>
{"name": "todo_write", "description": "Use this tool to create and manage a todo list for tracking complex multi-step tasks.\n\nUse proactively when:\n1. Complex multi-step tasks requiring 3+ distinct steps.\n2. Non-trivial tasks requiring careful planning.\n3. User explicitly requests a todo list.\n4. User provides multiple tasks.\n5. After receiving new instructions to capture requirements.\n\nSkip when:\n1. Single, straightforward task.\n2. Trivial task with no tracking benefit.\n3. Task completable in fewer than 3 trivial steps.\n4. Purely conversational or informational task.\n\nStatus values:\n- PENDING: Not yet started.\n- IN_PROGRESS: Currently working on.\n- COMPLETE: Finished all required outputs.\n- CANCELLED: No longer relevant.", "parameters": {"type": "object", "properties": {"todos": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "description": "Unique identifier for the todo item."}, "content": {"type": "string", "description": "The description/content of the todo item (max 70 characters recommended)."}, "status": {"type": "string", "enum": ["PENDING", "IN_PROGRESS", "COMPLETE", "CANCELLED"], "description": "The status of the todo item."}}}, "description": "Array of todo items (minimum 2 items recommended)."}, "merge": {"type": "boolean", "description": "Whether to merge the todos with existing. true=merge by id, false=replace all."}}, "required": ["merge", "todos"]}}
</function>

<function>
{"name": "TaskGet", "description": "Retrieve a task by its ID from the task list.\n\nWhen to use:\n- When you need the full description and context before starting work on a task.\n- To understand task dependencies (what it blocks, what blocks it).\n- After being assigned a task, to get complete requirements.\n\nOutput returns full task details: subject, description, status ('pending', 'in_progress', or 'completed'), blocks, and blockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to retrieve."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "TaskUpdate", "description": "Update a task in the task list.\n\nWhen to use:\n- Mark tasks as resolved when work is completed.\n- Update task details when requirements change.\n- Establish dependencies between tasks.\n\nIMPORTANT:\n- Always mark assigned tasks as resolved when finished.\n- ONLY mark a task as completed when FULLY accomplished.\n- If encountering errors or blockers, keep as in_progress.\n- Never mark as completed if tests are failing, implementation is partial, or unresolved errors exist.\n\nStatus progresses: pending → in_progress → completed.\n\nUpdatable fields: status, subject, description, activeForm, owner, metadata, addBlocks, addBlockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to update."}, "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled", "failed"], "description": "New status for the task."}, "subject": {"type": "string", "description": "New subject/title for the task (imperative form)."}, "description": {"type": "string", "description": "New description for the task."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress (e.g. 'Running tests')."}, "owner": {"type": "string", "description": "New owner for the task (agent name)."}, "metadata": {"type": "object", "description": "Metadata keys to merge into the task. Set a key to null to delete it."}, "addBlocks": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that this task blocks."}, "addBlockedBy": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that block this task."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "SendMessage", "description": "Send a message to another agent in the team.\n\nUse this tool to communicate with other agents (leader or experts). Messages are stored in the recipient's inbox and can be polled for new messages.\n\nCommon use cases:\n- Notify the leader about task completion.\n- Ask questions to other agents.\n- Share important findings or results.\n- Request help or clarification.\n\nThe recipient will receive the message when they check their inbox.", "parameters": {"type": "object", "properties": {"recipient": {"type": "string", "description": "The name of the recipient agent (e.g. 'leader', expert agent name)."}, "content": {"type": "string", "description": "The main content of the message."}, "summary": {"type": "string", "description": "A brief summary of the message (shown in notifications)."}, "type": {"type": "string", "enum": ["message"], "description": "The type of message. Use 'message' for standard messages."}}, "required": ["recipient", "content"]}}
</function>

<function>
{"name": "update_memory", "description": "Update, add, or delete memories to maintain memory consistency.\n\nWHEN TO USE:\n- When the user explicitly requests it.\n- When the user's intent is to add, delete, or modify a memory.\n\nBefore performing the operation, if the relevant memory does not already exist, use search_memory first to check the current memory status.\n\nMemory categories include:\n- User Preferences: user_info, user_hobby, user_communication\n- Project Information: project_tech_stack, project_build_configuration, project_dependency_configuration, project_ide_configuration, project_scm_configuration, project_environment_configuration, project_introduction\n- Development Standards: development_code_specification, development_practice_specification, development_test_specification, development_comment_specification\n- Lessons Learned: expert_experience, learned_skill_experience", "parameters": {"type": "object", "properties": {"title": {"type": "string", "description": "Descriptive title for a new or updated memory. Required when creating or updating."}, "content": {"type": "string", "description": "Content of a new or updated memory. Leave blank when deleting."}, "keywords": {"type": "string", "description": "Keywords to associate with the memory, comma-separated. Used for filtering and retrieval. Only used when creating."}, "category": {"type": "string", "description": "Memory category. Must be one of the predefined categories."}, "action": {"type": "string", "description": "The type of action: 'create', 'update', or 'delete'."}, "id": {"type": "string", "description": "ID of an existing memory to update or delete. Leave blank when creating."}, "scope": {"type": "string", "description": "Set to 'global' (cross-project) or 'workspace' (current project only). Only set when user explicitly requests."}}, "required": ["title", "content", "keywords", "category", "action"]}}
</function>

<function>
{"name": "search_memory", "description": "Retrieve detailed memories from the memory system.\n\nWHEN TO USE:\n- At any task node to recall relevant memories for efficient resolution.\n- At task start, retrieve background information and past experiences.\n- When using other retrieval tools, recall more relevant details.\n- If a tool execution fails, recall potential solutions.\n\nSearch approach:\n- First perform a shallow search for relevant keywords and categories.\n- If task is not completed after shallow search, perform a deep search.\n- When user explicitly requests recall or expresses doubt, use deep mode.\n\nDepth levels:\n- 'shallow': Smaller search scope, fewer but highly relevant memories. Use when task information is sufficient.\n- 'deep': Larger search scope, more comprehensive memories. Use when more information is needed or memory overview shows few relevant memories.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "What you want to recall. Keep it semantically clear, complete, and concise."}, "keywords": {"type": "string", "description": "Keywords describing the memory. Up to 5 keywords, each max 10 characters, comma-separated."}, "category": {"type": "string", "description": "Categories to search. Comma-separated. Use empty string to search all categories."}, "depth": {"type": "string", "description": "Recall depth: 'shallow' for focused results, 'deep' for comprehensive retrieval."}}, "required": ["query", "keywords", "category", "depth"]}}
</function>

</functions>


---
name: coding-expert-tools
description: coding-expert角色的完整工具定义
agent: coding-expert
tool_count: 20
---

<functions>

<function>
{"name": "read_file", "description": "Reads a file from the local filesystem. You can access any file directly by using this tool.\n\nUsage:\n- Provide the absolute path to the target file.\n- You can read the entire file by providing just the file path, which is recommended for better understanding and accuracy.\n- For very large files or when you need specific sections, you can optionally specify line ranges using start_line and end_line.\n- Supports reading image files (jpeg/jpg, png, webp).\n- If the file does not exist, an error will be returned.\n\nBest Practices:\n- Always ensure you have the COMPLETE context before making decisions.\n- If the retrieved range is insufficient, call this tool again to fetch additional content.\n- Use line ranges only when the file is too large to read at once.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the file to read."}, "start_line": {"type": "integer", "description": "The line number to start reading from, 1-based. Only provide if the file is too large to read at once."}, "end_line": {"type": "integer", "description": "The inclusive line number to end reading at, 1-based. Only provide if the file is too large to read at once."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "search_replace", "description": "Performs exact string replacements in files.\n\nUsage:\n- When editing text, ensure you preserve the exact indentation (tabs/spaces) as it appears before.\n- ALWAYS prefer editing existing files in the codebase. NEVER write new files unless explicitly required.\n- The edit will FAIL if old_string is not unique in the file. Either provide a larger string with more surrounding context to make it unique, or use replace_all to change every instance of old_string.\n- Use replace_all for replacing and renaming strings across the file. This parameter is useful if you want to rename a variable, for instance.\n- To create or overwrite a file, you should prefer the create_file tool.\n\nIMPORTANT:\n- old_string and new_string must be different.\n- Only use emojis if the user explicitly requests it.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The path to the file to modify. You can use either a relative path in the workspace or an absolute path."}, "old_string": {"type": "string", "description": "The text to replace. Must be an exact match of existing content in the file."}, "new_string": {"type": "string", "description": "The text to replace it with. Must be different from old_string."}, "replace_all": {"type": "boolean", "description": "If true, replace all occurrences of old_string in the file. Default is false."}}, "required": ["file_path", "old_string", "new_string"]}}
</function>

<function>
{"name": "create_file", "description": "Use this tool to create a new file with content or modify existing files by overwriting.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): Absolute path to the target file.\n- file_content (REQUIRED): The content of the file.\n- add_last_line_newline (OPTIONAL): Whether to add a newline at the end (default: true).\n\nIMPORTANT:\n- MUST LIMIT THE FILE CONTENT TO AT MOST 1000 LINES.\n- When editing existing files, MUST use search_replace tool FIRST, EXCEPT for COMPLETE file modification or overwrite.\n- ALWAYS prefer editing existing files in the codebase.\n- NEVER write new files unless explicitly required.\n- NEVER proactively create documentation files (*.md) or README files unless explicitly requested.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the target file. Must be generated first before any other arguments."}, "file_content": {"type": "string", "description": "The content of the file."}, "add_last_line_newline": {"type": "boolean", "description": "Whether to add a newline at the end of the file. Default is true."}}, "required": ["file_path", "file_content"]}}
</function>

<function>
{"name": "delete_file", "description": "Delete a file safely. ONLY use this tool to delete files.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): The absolute path to the file to delete.\n\nIMPORTANT:\n- This is the only safe tool to delete files.\n- DO NOT use shell commands or launch-process to remove files.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The absolute path to the file to delete."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "grep_code", "description": "High-performance file content search utilizing the ripgrep engine.\n\nGuidelines:\n- Recommended for precise string matching. Faster than terminal grep/rg and honors .gitignore automatically.\n- Full regex support available, such as \"error.*Handler\", \"class\\\\s+\\\\w+\". Special characters require escaping for exact matches, e.g. \"getUser\\\\(\".\n- Pattern format follows ripgrep conventions (not standard grep). Literal braces require escaping (e.g. map\\\\[string\\\\] to match map[string] in Go).\n- Broad glob patterns like '--glob *' should be avoided as they circumvent .gitignore and reduce performance.\n- Apply 'type' or 'glob' parameters only when file type is certain.\n- Results auto-expand to complete syntax blocks (functions, methods, structs) for better context.\n- Line matching: Single-line by default. Cross-line patterns require multiline: true flag.", "parameters": {"type": "object", "properties": {"regex": {"type": "string", "description": "Regex pattern for searching within file contents (rg --regexp)."}, "path": {"type": "string", "description": "Target file or directory for search (rg PATTERN [PATH ...]). Uses workspace roots by default."}, "type": {"type": "string", "description": "Specific file type for search (rg --type). Common options: js, py, rust, go, java, etc. Preferred over glob for standard types."}, "glob": {"type": "string", "description": "File filtering glob pattern (rg --glob GLOB -- PATH), such as \"*.java\" or \"*.{js,ts,tsx}\"."}, "multiline": {"type": "boolean", "description": "Activate multiline matching where . includes newlines and patterns cross line boundaries (rg -U --multiline-dotall). Default: false."}, "-A": {"type": "number", "description": "Lines of context to display after match (rg -A)."}, "-B": {"type": "number", "description": "Lines of context to display before match (rg -B)."}, "-C": {"type": "number", "description": "Lines of context to display both before and after match (rg -C)."}, "-i": {"type": "boolean", "description": "Perform case-insensitive search (rg -i). Default: false."}}, "required": ["regex"]}}
</function>

<function>
{"name": "search_file", "description": "Search for files by glob pattern (such as *.go or config/*.json) in workspace.\nONLY supports glob patterns, NOT regex. This only returns the paths of matching files. Limited to 25 results.\nMake your query more specific if you need to filter results further.\n\nExamples:\n- \"*.go\" (converted to \"**/*.go\") - matches all .go files\n- \"**/src/**\" - matches all files under src directories\n- \"**/test/**/test_*.py\" - matches test_*.py files within any test directory\n\nIf the pattern does not begin with \"**/\", it will be automatically prefixed with \"**/\" for recursive search.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Glob pattern for matching files."}, "path": {"type": "string", "description": "Directory path to search within. Supports both absolute paths and relative paths (resolved from workspace root). When omitted, searches from the workspace root."}}, "required": ["query"]}}
</function>

<function>
{"name": "list_dir", "description": "List the contents of a directory. Useful to understand the file structure before diving deeper into specific files.\n\nIMPORTANT:\n- The path parameter must be an absolute path.\n- Unless requested by the user, do not recursively check directories layer by layer; try to lock the directory location first before viewing.", "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Absolute path to the directory to list contents of."}}, "required": ["path"]}}
</function>

<function>
{"name": "run_in_terminal", "description": "Execute a shell command in the terminal.\n\nIMPORTANT: This tool is for terminal operations like git, npm, docker, etc. DO NOT use it for file operations (reading, writing, editing, searching, finding files) - use the specialized tools instead.\n\nRules:\n1. Ensure commands are compatible with the user's operating system.\n2. Working directory inherits from previous step if shell reuse is possible; otherwise uses workspace root. Use absolute paths and avoid cd.\n3. For long-running commands or those expected to keep running, set is_background to true. Use get_terminal_output to check background process output later.\n4. For commands requiring user interaction, pass non-interactive flags (e.g. --yes).\n5. Do not delete directories or files through commands.\n6. Do not use commands that may harm the user's OS (e.g. rm, sudo, wget, curl).\n7. Do not use newline characters in the command.\n8. Commands must not exceed 500 characters. Write longer commands to a file first.\n9. Assess risk before execution. Read-only commands are generally safe; destructive commands (rm, wget) are risky.\n10. NEVER update git config or run destructive/irreversible git commands unless explicitly requested.\n11. NEVER commit changes unless explicitly asked.", "parameters": {"type": "object", "properties": {"command": {"type": "string", "description": "REQUIRED. The command to run in the terminal."}, "is_background": {"type": "boolean", "description": "REQUIRED. Whether the command starts a background process. If true, runs in background and returns a terminal ID for later output checking."}, "timeout": {"type": "integer", "description": "Optional. Timeout in milliseconds for command completion (default 180000, max 1800000). Foreground commands exceeding timeout are auto-converted to background."}, "has_risk": {"type": "boolean", "description": "Whether the command is potentially risky (e.g. destructive operations)."}, "required_permissions": {"type": "string", "description": "Optional. Set to 'all' to request running outside sandbox. Only use when confirmed failure was caused by sandbox restrictions."}, "command_names": {"type": "array", "items": {"type": "string"}, "description": "Optional. Parsed command names from the command (e.g. [\"cd\", \"ls\"])."}}, "required": ["command", "is_background"]}}
</function>

<function>
{"name": "get_terminal_output", "description": "Get the output of a terminal command previously started with run_in_terminal. The tool will intelligently wait for command completion based on the wait_seconds parameter.\n\nTips:\n- Choose wait_seconds based on command type: 2s for quick commands (ls, cat), 15s for compilation/tests, 30s for application startup.\n- Default wait is 2s.", "parameters": {"type": "object", "properties": {"terminal_id": {"type": "string", "description": "The ID of the terminal command output to check."}, "wait_seconds": {"type": "integer", "description": "Wait time in seconds for command completion. Default is 2."}}, "required": ["terminal_id"]}}
</function>

<function>
{"name": "search_codebase", "description": "Semantic code search that finds code by meaning, not exact text. Use when you don't know which files contain needed info, or to ask high-level 'where/how' questions about the codebase.\n\nExamples: \"authentication logic\", \"how payments work\", \"database connection handling\".\n\nDecision Rule: If query contains PascalCase, camelCase, or 'class/interface/method + Name', use search_symbol instead.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "A concise, keyword-rich search string optimized for code retrieval. Should contain technical terms, relevant programming concepts, and implementation-focused language. Emphasize code functionality, architecture components, and technical specifications."}, "key_words": {"type": "string", "description": "Extract maximum three most important keywords from the query, ranked by relevance. Prioritize keywords most relevant to the query's core intent. Ignore generic programming words. Format: keyword1,keyword2."}, "target_directories": {"type": "array", "items": {"type": "string"}, "description": "Absolute directory paths to narrow the search scope. Each path must be a workspace root or subdirectory. Glob patterns are not supported."}}, "required": ["query", "key_words"]}}
</function>

<function>
{"name": "search_symbol", "description": "Tool for discovering code symbols and their relationships.\n\nWHEN TO USE:\n- For class names (e.g. \"PmsProduct\")\n- For method names (e.g. \"UserService.createUser\", \"createUser\")\n- For any visible symbol names in queries/messages\n- Discover related symbols through relationships\n\nUsage:\n- Accepts array of search objects via \"queries\".\n- Each query needs \"symbol\" (exact name to search).\n- Optional: \"file_path\", \"relation\".\n\nRelation filter options:\n- calls: Symbols called by the current symbol\n- called_by: Symbols that call the current symbol\n- references: Symbols referenced by the current symbol\n- referenced_by: Symbols that reference the current symbol\n- extends: Symbols extended by the current symbol\n- extended_by: Symbols that extend the current symbol\n- implements: Interfaces implemented by the current symbol\n- implemented_by: Symbols that implement the current symbol\n- contains: Symbols contained within the current symbol\n- contained_by: Symbols that contain the current symbol\n- overrides: Methods overridden by the current symbol\n- overridden_by: Symbols that override the current symbol\n- all: All relationships\n- none: Only symbol definitions, no relationships\n\nIMPORTANT: Avoid using 'all' relation when possible. Empty result means no matches found.", "parameters": {"type": "object", "properties": {"queries": {"type": "array", "items": {"type": "object", "properties": {"symbol": {"type": "string", "description": "The symbol name to search for. One symbol per object."}, "relation": {"type": "string", "enum": ["calls", "called_by", "references", "referenced_by", "extends", "extended_by", "implements", "implemented_by", "contains", "contained_by", "overrides", "overridden_by", "all", "none"], "description": "The relationship to retrieve for this symbol."}, "file_path": {"type": "string", "description": "Optional file path to narrow the search."}}}}, "description": "Array of search objects, each describing a single symbol lookup."}, "required": ["queries"]}}
</function>

<function>
{"name": "fetch_content", "description": "Fetches the main content from a web page. The URL must be an HTTP or HTTPS URL pointing to a valid internet resource accessible via web browser.\n\nUseful for summarizing or analyzing the content of a webpage. Use this tool when looking for information from a specific webpage.", "parameters": {"type": "object", "properties": {"url": {"type": "string", "description": "URL to read content from. Must start with http:// or https://."}, "query": {"type": "string", "description": "The query to search for in the web page's content. Should be a clear and concise description of the content you want to find."}}, "required": ["url"]}}
</function>

<function>
{"name": "search_web", "description": "Explore the web for real-time information on any topic.\n\nUse this tool when you need up-to-date information that might not be included in existing knowledge, or when you need to verify current facts. The search results will include relevant snippets and URLs from web pages.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Search query. Value range: 1 to 100 characters."}, "timeRange": {"type": "string", "enum": ["OneDay", "OneWeek", "OneMonth", "OneYear", "NoLimit"], "description": "The time range of the query. Default is NoLimit."}}, "required": ["query"]}}
</function>

<function>
{"name": "get_problems", "description": "Get any compile or lint errors in a code file. If the user mentions errors or problems in a file, they may be referring to these. Use the tool to see the same errors that the user is seeing.", "parameters": {"type": "object", "properties": {"file_paths": {"type": "array", "items": {"type": "string", "description": "The absolute path of a file."}, "description": "The absolute paths of mentioned or edited files. Multiple files supported."}}, "required": ["file_paths"]}}
</function>

<function>
{"name": "todo_write", "description": "Use this tool to create and manage a todo list for tracking complex multi-step tasks.\n\nUse proactively when:\n1. Complex multi-step tasks requiring 3+ distinct steps.\n2. Non-trivial tasks requiring careful planning.\n3. User explicitly requests a todo list.\n4. User provides multiple tasks.\n5. After receiving new instructions to capture requirements.\n\nSkip when:\n1. Single, straightforward task.\n2. Trivial task with no tracking benefit.\n3. Task completable in fewer than 3 trivial steps.\n4. Purely conversational or informational task.\n\nStatus values:\n- PENDING: Not yet started.\n- IN_PROGRESS: Currently working on.\n- COMPLETE: Finished all required outputs.\n- CANCELLED: No longer relevant.", "parameters": {"type": "object", "properties": {"todos": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "description": "Unique identifier for the todo item."}, "content": {"type": "string", "description": "The description/content of the todo item (max 70 characters recommended)."}, "status": {"type": "string", "enum": ["PENDING", "IN_PROGRESS", "COMPLETE", "CANCELLED"], "description": "The status of the todo item."}}}, "description": "Array of todo items (minimum 2 items recommended)."}, "merge": {"type": "boolean", "description": "Whether to merge the todos with existing. true=merge by id, false=replace all."}}, "required": ["merge", "todos"]}}
</function>

<function>
{"name": "TaskGet", "description": "Retrieve a task by its ID from the task list.\n\nWhen to use:\n- When you need the full description and context before starting work on a task.\n- To understand task dependencies (what it blocks, what blocks it).\n- After being assigned a task, to get complete requirements.\n\nOutput returns full task details: subject, description, status ('pending', 'in_progress', or 'completed'), blocks, and blockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to retrieve."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "TaskUpdate", "description": "Update a task in the task list.\n\nWhen to use:\n- Mark tasks as resolved when work is completed.\n- Update task details when requirements change.\n- Establish dependencies between tasks.\n\nIMPORTANT:\n- Always mark assigned tasks as resolved when finished.\n- ONLY mark a task as completed when FULLY accomplished.\n- If encountering errors or blockers, keep as in_progress.\n- Never mark as completed if tests are failing, implementation is partial, or unresolved errors exist.\n\nStatus progresses: pending → in_progress → completed.\n\nUpdatable fields: status, subject, description, activeForm, owner, metadata, addBlocks, addBlockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to update."}, "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled", "failed"], "description": "New status for the task."}, "subject": {"type": "string", "description": "New subject/title for the task (imperative form)."}, "description": {"type": "string", "description": "New description for the task."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress (e.g. 'Running tests')."}, "owner": {"type": "string", "description": "New owner for the task (agent name)."}, "metadata": {"type": "object", "description": "Metadata keys to merge into the task. Set a key to null to delete it."}, "addBlocks": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that this task blocks."}, "addBlockedBy": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that block this task."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "SendMessage", "description": "Send a message to another agent in the team.\n\nUse this tool to communicate with other agents (leader or experts). Messages are stored in the recipient's inbox and can be polled for new messages.\n\nCommon use cases:\n- Notify the leader about task completion.\n- Ask questions to other agents.\n- Share important findings or results.\n- Request help or clarification.\n\nThe recipient will receive the message when they check their inbox.", "parameters": {"type": "object", "properties": {"recipient": {"type": "string", "description": "The name of the recipient agent (e.g. 'leader', expert agent name)."}, "content": {"type": "string", "description": "The main content of the message."}, "summary": {"type": "string", "description": "A brief summary of the message (shown in notifications)."}, "type": {"type": "string", "enum": ["message"], "description": "The type of message. Use 'message' for standard messages."}}, "required": ["recipient", "content"]}}
</function>

<function>
{"name": "update_memory", "description": "Update, add, or delete memories to maintain memory consistency.\n\nWHEN TO USE:\n- When the user explicitly requests it.\n- When the user's intent is to add, delete, or modify a memory.\n\nBefore performing the operation, if the relevant memory does not already exist, use search_memory first to check the current memory status.\n\nMemory categories include:\n- User Preferences: user_info, user_hobby, user_communication\n- Project Information: project_tech_stack, project_build_configuration, project_dependency_configuration, project_ide_configuration, project_scm_configuration, project_environment_configuration, project_introduction\n- Development Standards: development_code_specification, development_practice_specification, development_test_specification, development_comment_specification\n- Lessons Learned: expert_experience, learned_skill_experience", "parameters": {"type": "object", "properties": {"title": {"type": "string", "description": "Descriptive title for a new or updated memory. Required when creating or updating."}, "content": {"type": "string", "description": "Content of a new or updated memory. Leave blank when deleting."}, "keywords": {"type": "string", "description": "Keywords to associate with the memory, comma-separated. Used for filtering and retrieval. Only used when creating."}, "category": {"type": "string", "description": "Memory category. Must be one of the predefined categories."}, "action": {"type": "string", "description": "The type of action: 'create', 'update', or 'delete'."}, "id": {"type": "string", "description": "ID of an existing memory to update or delete. Leave blank when creating."}, "scope": {"type": "string", "description": "Set to 'global' (cross-project) or 'workspace' (current project only). Only set when user explicitly requests."}}, "required": ["title", "content", "keywords", "category", "action"]}}
</function>

<function>
{"name": "search_memory", "description": "Retrieve detailed memories from the memory system.\n\nWHEN TO USE:\n- At any task node to recall relevant memories for efficient resolution.\n- At task start, retrieve background information and past experiences.\n- When using other retrieval tools, recall more relevant details.\n- If a tool execution fails, recall potential solutions.\n\nSearch approach:\n- First perform a shallow search for relevant keywords and categories.\n- If task is not completed after shallow search, perform a deep search.\n- When user explicitly requests recall or expresses doubt, use deep mode.\n\nDepth levels:\n- 'shallow': Smaller search scope, fewer but highly relevant memories. Use when task information is sufficient.\n- 'deep': Larger search scope, more comprehensive memories. Use when more information is needed or memory overview shows few relevant memories.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "What you want to recall. Keep it semantically clear, complete, and concise."}, "keywords": {"type": "string", "description": "Keywords describing the memory. Up to 5 keywords, each max 10 characters, comma-separated."}, "category": {"type": "string", "description": "Categories to search. Comma-separated. Use empty string to search all categories."}, "depth": {"type": "string", "description": "Recall depth: 'shallow' for focused results, 'deep' for comprehensive retrieval."}}, "required": ["query", "keywords", "category", "depth"]}}
</function>

</functions>
---
name: code-review-expert-tools
agent: code-review-expert
tool_count: 20
---

<functions>
<function>
{"name": "read_file", "description": "Reads a file from the local filesystem. You can access any file directly by using this tool. Supports reading entire files or specific line ranges. Also supports reading image files (jpeg/jpg, png, webp).\n\nUsage:\n- Provide just the file path to read the entire file.\n- Optionally specify start_line and end_line to read a specific range of lines.\n- For very large files, use line ranges to read in chunks.\n- Can also read image files when called with the appropriate path.", "parameters": {"type": "object", "required": ["file_path"], "properties": {"file_path": {"type": "string", "description": "Absolute path to the file to read."}, "start_line": {"type": "integer", "description": "The line number to start reading from, 1-based. Only provide if the file is too large to read at once."}, "end_line": {"type": "integer", "description": "The inclusive line number to end reading at, 1-based. Only provide if the file is too large to read at once."}}}}
</function>
<function>
{"name": "grep_code", "description": "High-performance file content search utilizing the ripgrep engine. Supports full regex, automatic .gitignore honoring, and auto-expansion of matched results to complete syntax blocks (functions, methods, structs) for better context.\n\nGuidelines:\n- Recommended for precise string matching.\n- Full regex support (e.g. \"error.*Handler\", \"class\\\\s+\\\\w+\"). Special characters require escaping for exact matches.\n- Pattern format follows ripgrep conventions.\n- Apply 'type' or 'glob' parameters only when file type is certain.\n- Results auto-expand to complete syntax blocks for better context.\n- Single-line matching by default. Cross-line patterns require multiline: true.", "parameters": {"type": "object", "required": ["regex"], "properties": {"regex": {"type": "string", "description": "Regex pattern for searching within file contents (rg --regexp)."}, "path": {"type": "string", "description": "Target file or directory for search. Uses workspace roots by default."}, "type": {"type": "string", "description": "Specific file type for search (rg --type). Common options: js, py, rust, go, java, etc."}, "glob": {"type": "string", "description": "File filtering glob pattern (rg --glob GLOB), such as \"*.java\" or \"*.{js,ts,tsx}\"."}, "multiline": {"type": "boolean", "description": "Activate multiline matching where . includes newlines and patterns cross line boundaries. Default: false."}, "-A": {"type": "number", "description": "Lines of context to display after match (rg -A)."}, "-B": {"type": "number", "description": "Lines of context to display before match (rg -B)."}, "-C": {"type": "number", "description": "Lines of context to display both before and after match (rg -C)."}, "-i": {"type": "boolean", "description": "Perform case-insensitive search (rg -i). Defaults to false."}}}}
</function>
<function>
{"name": "search_file", "description": "Search for files by glob pattern (such as *.go or config/*.json) in workspace. Only supports glob patterns, NOT regex. Returns the paths of matching files. Limited to 25 results.\n\nExamples:\n- \"*.go\" (converted to \"**/*.go\") - matches all .go files\n- \"**/src/**\" - matches all files under src directories\n- \"**/test/**/test_*.py\" - matches test_*.py files within any test directory", "parameters": {"type": "object", "required": ["query"], "properties": {"query": {"type": "string", "description": "Glob pattern for matching files. If the pattern does not begin with \"**/\", it will be automatically prefixed with \"**/\" for recursive search."}, "path": {"type": "string", "description": "Directory path to search within. Supports both absolute and relative paths."}}}}
</function>
<function>
{"name": "run_in_terminal", "description": "Execute a shell command in the terminal. Use for terminal operations like git, npm, docker, build commands, test runners, etc.\n\nRules:\n- For long-running or persistent processes, set is_background to true.\n- Background terminals return a terminal_id for later output checking via get_terminal_output.\n- Use absolute paths and avoid cd when possible.\n- Do not use newline characters in the command.\n- Commands must not exceed 500 characters.", "parameters": {"type": "object", "required": ["command", "is_background"], "properties": {"command": {"type": "string", "description": "The command to run in the terminal."}, "is_background": {"type": "boolean", "description": "Whether the command starts a background process. If true, the command runs in the background and returns a terminal_id."}, "timeout": {"type": "integer", "description": "Timeout in milliseconds for the command to complete (default 180000, max 1800000)."}, "has_risk": {"type": "boolean", "description": "Whether the command is potentially risky."}, "required_permissions": {"type": "string", "description": "Set to 'all' to request running outside the sandbox."}, "command_names": {"type": "array", "items": {"type": "string"}, "description": "Parsed command names from the command."}}}}
</function>
<function>
{"name": "search_codebase", "description": "Semantic code search that finds code by meaning, not exact text. Use when you need to understand how something works, where functionality is implemented, or explore unfamiliar codebases. Finds relevant code across the entire project based on natural language queries.\n\nExamples: \"authentication logic\", \"how payments work\", \"database connection handling\".", "parameters": {"type": "object", "required": ["query", "key_words"], "properties": {"query": {"type": "string", "description": "A concise, keyword-rich search string optimized for code retrieval. Should contain technical terms, relevant programming concepts, and implementation-focused language."}, "key_words": {"type": "string", "description": "Extract maximum three most important keywords from the query, ranked by relevance. Format: keyword1,keyword2,keyword3"}, "target_directories": {"type": "array", "items": {"type": "string"}, "description": "Absolute directory paths to narrow the search scope."}}}}
</function>
<function>
{"name": "search_symbol", "description": "Tool for discovering code symbols and their relationships. Use for class names, method names, variable names, or any visible symbol names. Supports relationship queries such as calls, called_by, references, referenced_by, extends, extended_by, implements, implemented_by, contains, contained_by, overrides, overridden_by, all, none.", "parameters": {"type": "object", "required": ["queries"], "properties": {"queries": {"type": "array", "items": {"type": "object", "properties": {"symbol": {"type": "string", "description": "The symbol name to search for. One symbol per object."}, "file_path": {"type": "string", "description": "Optional file path to narrow the search."}, "relation": {"type": "string", "enum": ["calls", "called_by", "references", "referenced_by", "extends", "extended_by", "implements", "implemented_by", "contains", "contained_by", "overrides", "overridden_by", "all", "none"], "description": "The relationship to retrieve for this symbol."}}}, "description": "Array of search objects describing symbol lookups."}}}}
</function>

<function>
{"name": "list_dir", "description": "List the contents of a directory. Useful to understand the file structure before diving deeper into specific files. The path parameter must be an absolute path.", "parameters": {"type": "object", "required": ["path"], "properties": {"path": {"type": "string", "description": "Absolute path to list contents of."}}}}
</function>
<function>
{"name": "get_terminal_output", "description": "Get the output of a terminal command previously started with run_in_terminal. The tool will intelligently wait for command completion based on the wait_seconds parameter.", "parameters": {"type": "object", "required": ["terminal_id"], "properties": {"terminal_id": {"type": "string", "description": "The ID of the terminal command output to check."}, "wait_seconds": {"type": "integer", "description": "Wait time in seconds for command completion. Choose based on command type: 2s for quick commands, 15s for compilation/tests, 30s for app startup. Default is 2s."}}}}
</function>
<function>
{"name": "todo_write", "description": "Create and manage a todo list for tracking complex multi-step tasks. Use proactively for complex tasks (3+ steps), non-trivial tasks, user-requested lists, or multiple tasks.\n\nStatus values: PENDING, IN_PROGRESS, COMPLETE, CANCELLED.", "parameters": {"type": "object", "required": ["merge", "todos"], "properties": {"merge": {"type": "boolean", "description": "Whether to merge the todos with existing. true=merge by id, false=replace all."}, "todos": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "description": "Unique identifier for the todo item."}, "content": {"type": "string", "description": "The description/content of the todo item."}, "status": {"type": "string", "enum": ["PENDING", "IN_PROGRESS", "COMPLETE", "CANCELLED"], "description": "The status of the todo item."}}, "required": ["id", "content", "status"]}, "description": "Array of todo items (minimum 2 items recommended)."}}}}
</function>
<function>
{"name": "TaskGet", "description": "Retrieve a task by its ID from the task list. Returns full task details including subject, description, status, blocks, and blockedBy.", "parameters": {"type": "object", "required": ["taskId"], "properties": {"taskId": {"type": "string", "description": "The ID of the task to retrieve."}}}}
</function>
<function>
{"name": "TaskUpdate", "description": "Update a task in the task list. Use to mark tasks as resolved, update details, or establish dependencies.\n\nUpdatable fields: status, subject, description, activeForm, owner, metadata, addBlocks, addBlockedBy.\n\nStatus progresses: pending → in_progress → completed.", "parameters": {"type": "object", "required": ["taskId"], "properties": {"taskId": {"type": "string", "description": "The ID of the task to update."}, "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled", "failed"], "description": "New status for the task."}, "subject": {"type": "string", "description": "New subject for the task (imperative form)."}, "description": {"type": "string", "description": "New description for the task."}, "owner": {"type": "string", "description": "New owner for the task (agent name)."}, "addBlocks": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that this task blocks."}, "addBlockedBy": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that block this task."}, "metadata": {"type": "object", "description": "Metadata keys to merge into the task."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress."}}}}
</function>
<function>
{"name": "SendMessage", "description": "Send a message to another agent in the team. Use to communicate with other agents (leader or experts) — notify about task completion, ask questions, share findings, or request clarification.", "parameters": {"type": "object", "required": ["recipient", "content"], "properties": {"recipient": {"type": "string", "description": "The name of the recipient agent (e.g., 'leader', expert agent name)."}, "content": {"type": "string", "description": "The main content of the message."}, "summary": {"type": "string", "description": "A brief summary of the message (shown in notifications)."}, "type": {"type": "string", "enum": ["message"], "description": "The type of message. Use 'message' for standard messages."}}}}
</function>
<function>
{"name": "update_memory", "description": "Update, add, or delete memories based on the user's intent to maintain memory consistency. Supports creating, updating, and deleting memories across various categories.\n\nMemory categories include: user_info, user_hobby, user_communication, project_tech_stack, project_build_configuration, project_dependency_configuration, project_ide_configuration, project_scm_configuration, project_environment_configuration, development_code_specification, development_practice_specification, development_test_specification, development_comment_specification, expert_experience, learned_skill_experience.", "parameters": {"type": "object", "required": ["title", "content", "keywords", "category", "action"], "properties": {"title": {"type": "string", "description": "Descriptive title for the memory."}, "content": {"type": "string", "description": "Content of the memory."}, "keywords": {"type": "string", "description": "Keywords associated with the memory, comma-separated."}, "category": {"type": "string", "description": "Memory category (e.g., user_info, project_tech_stack, expert_experience, etc.)."}, "action": {"type": "string", "description": "Action type: 'create', 'update', or 'delete'."}, "id": {"type": "string", "description": "ID of an existing memory to update or delete."}, "scope": {"type": "string", "description": "Memory scope: 'global' (cross-project) or 'workspace' (current project only)."}}}}
</function>
<function>
{"name": "search_memory", "description": "Retrieve detailed memories from the memory system. Use to recall relevant information, background context, past experiences, and known solutions.\n\nDepth levels:\n- 'shallow': Smaller scope, fewer but highly relevant results.\n- 'deep': Larger scope, more comprehensive results.", "parameters": {"type": "object", "required": ["query", "keywords", "category", "depth"], "properties": {"query": {"type": "string", "description": "What you want to recall. Keep it semantically clear, complete, and concise."}, "keywords": {"type": "string", "description": "Up to 5 keywords, comma-separated. Each keyword should not exceed 10 characters."}, "category": {"type": "string", "description": "Memory categories to search, comma-separated. Use empty string to search all categories."}, "depth": {"type": "string", "description": "Recall depth: 'shallow' for focused results, 'deep' for comprehensive retrieval."}}}}
</function>
<function>
{"name": "create_file", "description": "Create a new file with content or overwrite an existing file. Use this tool to write new files or completely replace existing file contents.\n\nRequirements:\n- file_path must be an absolute path.\n- File content must not exceed 1000 lines.\n- Always prefer editing existing files over creating new ones.\n- Never proactively create documentation files unless explicitly requested.", "parameters": {"type": "object", "required": ["file_path", "file_content"], "properties": {"file_path": {"type": "string", "description": "Absolute path to the target file."}, "file_content": {"type": "string", "description": "The content of the file."}, "add_last_line_newline": {"type": "boolean", "description": "Whether to add a newline at the end of the file. Default: true."}}}}
</function>
<function>
{"name": "delete_file", "description": "Delete a file safely. This is the only safe tool to delete files. Do not use shell commands to remove files.\n\nRequirements:\n- file_path must be an absolute path.", "parameters": {"type": "object", "required": ["file_path"], "properties": {"file_path": {"type": "string", "description": "The absolute file path to delete."}}}}
</function>
<function>
{"name": "search_replace", "description": "Performs exact string replacements in files. Use for editing existing files by replacing specific text with new text.\n\nRules:\n- Preserve exact indentation (tabs/spaces) as it appears in the original.\n- The edit will FAIL if old_string is not unique in the file. Provide more surrounding context to make it unique, or use replace_all.\n- Use replace_all for renaming variables or replacing all occurrences.", "parameters": {"type": "object", "required": ["file_path", "old_string", "new_string"], "properties": {"file_path": {"type": "string", "description": "The path to the file to modify."}, "old_string": {"type": "string", "description": "The text to replace. Must be unique in the file unless replace_all is true."}, "new_string": {"type": "string", "description": "The text to replace it with (must be different from old_string)."}, "replace_all": {"type": "boolean", "description": "Replace all occurrences of old_string. Default: false."}}}}
</function>
<function>
{"name": "fetch_content", "description": "Fetches the main content from a web page. Useful for summarizing or analyzing the content of a webpage. The URL must be an HTTP or HTTPS URL pointing to a valid internet resource.", "parameters": {"type": "object", "required": ["url"], "properties": {"url": {"type": "string", "description": "URL to read content from. Must start with http:// or https://."}, "query": {"type": "string", "description": "Optional query to search for in the web page's content. Should be a clear and concise description of the content you want to find."}}}}
</function>
<function>
{"name": "search_web", "description": "Explore the web for real-time information on any topic. Use when you need up-to-date information that might not be in existing knowledge, or when you need to verify current facts. Returns relevant snippets and URLs from web pages.", "parameters": {"type": "object", "required": ["query"], "properties": {"query": {"type": "string", "description": "Search query. Value range: 1 to 100 characters."}, "timeRange": {"type": "string", "enum": ["OneDay", "OneWeek", "OneMonth", "OneYear", "NoLimit"], "description": "The time range of the query. Default is NoLimit."}}}}
</function>
<function>
{"name": "get_problems", "description": "Get any compile or lint errors in a code file. If the user mentions errors or problems in a file, they may be referring to these. Use this tool to see the same errors the user is seeing.", "parameters": {"type": "object", "required": ["file_paths"], "properties": {"file_paths": {"type": "array", "items": {"type": "string", "description": "The absolute path of a file to check."}, "description": "The absolute paths of mentioned or edited files."}}}}
</function>
</functions>
---
name: browser-expert-tools
description: browser-expert角色的完整工具定义
agent: browser-expert
tool_count: 20
---

<functions>

<function>
{"name": "read_file", "description": "Reads a file from the local filesystem. You can access any file directly by using this tool.\n\nUsage:\n- Provide the absolute path to the target file.\n- You can read the entire file by providing just the file path, which is recommended for better understanding and accuracy.\n- For very large files or when you need specific sections, you can optionally specify line ranges using start_line and end_line.\n- Supports reading image files (jpeg/jpg, png, webp).\n- If the file does not exist, an error will be returned.\n\nBest Practices:\n- Always ensure you have the COMPLETE context before making decisions.\n- If the retrieved range is insufficient, call this tool again to fetch additional content.\n- Use line ranges only when the file is too large to read at once.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the file to read."}, "start_line": {"type": "integer", "description": "The line number to start reading from, 1-based. Only provide if the file is too large to read at once."}, "end_line": {"type": "integer", "description": "The inclusive line number to end reading at, 1-based. Only provide if the file is too large to read at once."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "search_replace", "description": "Performs exact string replacements in files.\n\nUsage:\n- When editing text, ensure you preserve the exact indentation (tabs/spaces) as it appears before.\n- ALWAYS prefer editing existing files in the codebase. NEVER write new files unless explicitly required.\n- The edit will FAIL if old_string is not unique in the file. Either provide a larger string with more surrounding context to make it unique, or use replace_all to change every instance of old_string.\n- Use replace_all for replacing and renaming strings across the file. This parameter is useful if you want to rename a variable, for instance.\n- To create or overwrite a file, you should prefer the create_file tool.\n\nIMPORTANT:\n- old_string and new_string must be different.\n- Only use emojis if the user explicitly requests it.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The path to the file to modify. You can use either a relative path in the workspace or an absolute path."}, "old_string": {"type": "string", "description": "The text to replace. Must be an exact match of existing content in the file."}, "new_string": {"type": "string", "description": "The text to replace it with. Must be different from old_string."}, "replace_all": {"type": "boolean", "description": "If true, replace all occurrences of old_string in the file. Default is false."}}, "required": ["file_path", "old_string", "new_string"]}}
</function>

<function>
{"name": "create_file", "description": "Use this tool to create a new file with content or modify existing files by overwriting.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): Absolute path to the target file.\n- file_content (REQUIRED): The content of the file.\n- add_last_line_newline (OPTIONAL): Whether to add a newline at the end (default: true).\n\nIMPORTANT:\n- MUST LIMIT THE FILE CONTENT TO AT MOST 1000 LINES.\n- When editing existing files, MUST use search_replace tool FIRST, EXCEPT for COMPLETE file modification or overwrite.\n- ALWAYS prefer editing existing files in the codebase.\n- NEVER write new files unless explicitly required.\n- NEVER proactively create documentation files (*.md) or README files unless explicitly requested.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the target file. Must be generated first before any other arguments."}, "file_content": {"type": "string", "description": "The content of the file."}, "add_last_line_newline": {"type": "boolean", "description": "Whether to add a newline at the end of the file. Default is true."}}, "required": ["file_path", "file_content"]}}
</function>

<function>
{"name": "delete_file", "description": "Delete a file safely. ONLY use this tool to delete files.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): The absolute path to the file to delete.\n\nIMPORTANT:\n- This is the only safe tool to delete files.\n- DO NOT use shell commands or launch-process to remove files.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The absolute path to the file to delete."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "grep_code", "description": "High-performance file content search utilizing the ripgrep engine.\n\nGuidelines:\n- Recommended for precise string matching. Faster than terminal grep/rg and honors .gitignore automatically.\n- Full regex support available, such as \"error.*Handler\", \"class\\\\s+\\\\w+\". Special characters require escaping for exact matches, e.g. \"getUser\\\\(\".\n- Pattern format follows ripgrep conventions (not standard grep). Literal braces require escaping (e.g. map\\\\[string\\\\] to match map[string] in Go).\n- Broad glob patterns like '--glob *' should be avoided as they circumvent .gitignore and reduce performance.\n- Apply 'type' or 'glob' parameters only when file type is certain.\n- Results auto-expand to complete syntax blocks (functions, methods, structs) for better context.\n- Line matching: Single-line by default. Cross-line patterns require multiline: true flag.", "parameters": {"type": "object", "properties": {"regex": {"type": "string", "description": "Regex pattern for searching within file contents (rg --regexp)."}, "path": {"type": "string", "description": "Target file or directory for search (rg PATTERN [PATH ...]). Uses workspace roots by default."}, "type": {"type": "string", "description": "Specific file type for search (rg --type). Common options: js, py, rust, go, java, etc. Preferred over glob for standard types."}, "glob": {"type": "string", "description": "File filtering glob pattern (rg --glob GLOB -- PATH), such as \"*.java\" or \"*.{js,ts,tsx}\"."}, "multiline": {"type": "boolean", "description": "Activate multiline matching where . includes newlines and patterns cross line boundaries (rg -U --multiline-dotall). Default: false."}, "-A": {"type": "number", "description": "Lines of context to display after match (rg -A)."}, "-B": {"type": "number", "description": "Lines of context to display before match (rg -B)."}, "-C": {"type": "number", "description": "Lines of context to display both before and after match (rg -C)."}, "-i": {"type": "boolean", "description": "Perform case-insensitive search (rg -i). Default: false."}}, "required": ["regex"]}}
</function>

<function>
{"name": "search_file", "description": "Search for files by glob pattern (such as *.go or config/*.json) in workspace.\nONLY supports glob patterns, NOT regex. This only returns the paths of matching files. Limited to 25 results.\nMake your query more specific if you need to filter results further.\n\nExamples:\n- \"*.go\" (converted to \"**/*.go\") - matches all .go files\n- \"**/src/**\" - matches all files under src directories\n- \"**/test/**/test_*.py\" - matches test_*.py files within any test directory\n\nIf the pattern does not begin with \"**/\", it will be automatically prefixed with \"**/\" for recursive search.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Glob pattern for matching files."}, "path": {"type": "string", "description": "Directory path to search within. Supports both absolute paths and relative paths (resolved from workspace root). When omitted, searches from the workspace root."}}, "required": ["query"]}}
</function>

<function>
{"name": "list_dir", "description": "List the contents of a directory. Useful to understand the file structure before diving deeper into specific files.\n\nIMPORTANT:\n- The path parameter must be an absolute path.\n- Unless requested by the user, do not recursively check directories layer by layer; try to lock the directory location first before viewing.", "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Absolute path to the directory to list contents of."}}, "required": ["path"]}}
</function>

<function>
{"name": "run_in_terminal", "description": "Execute a shell command in the terminal.\n\nIMPORTANT: This tool is for terminal operations like git, npm, docker, etc. DO NOT use it for file operations (reading, writing, editing, searching, finding files) - use the specialized tools instead.\n\nRules:\n1. Ensure commands are compatible with the user's operating system.\n2. Working directory inherits from previous step if shell reuse is possible; otherwise uses workspace root. Use absolute paths and avoid cd.\n3. For long-running commands or those expected to keep running, set is_background to true. Use get_terminal_output to check background process output later.\n4. For commands requiring user interaction, pass non-interactive flags (e.g. --yes).\n5. Do not delete directories or files through commands.\n6. Do not use commands that may harm the user's OS (e.g. rm, sudo, wget, curl).\n7. Do not use newline characters in the command.\n8. Commands must not exceed 500 characters. Write longer commands to a file first.\n9. Assess risk before execution. Read-only commands are generally safe; destructive commands (rm, wget) are risky.\n10. NEVER update git config or run destructive/irreversible git commands unless explicitly requested.\n11. NEVER commit changes unless explicitly asked.", "parameters": {"type": "object", "properties": {"command": {"type": "string", "description": "REQUIRED. The command to run in the terminal."}, "is_background": {"type": "boolean", "description": "REQUIRED. Whether the command starts a background process. If true, runs in background and returns a terminal ID for later output checking."}, "timeout": {"type": "integer", "description": "Optional. Timeout in milliseconds for command completion (default 180000, max 1800000). Foreground commands exceeding timeout are auto-converted to background."}, "has_risk": {"type": "boolean", "description": "Whether the command is potentially risky (e.g. destructive operations)."}, "required_permissions": {"type": "string", "description": "Optional. Set to 'all' to request running outside sandbox. Only use when confirmed failure was caused by sandbox restrictions."}, "command_names": {"type": "array", "items": {"type": "string"}, "description": "Optional. Parsed command names from the command (e.g. [\"cd\", \"ls\"])."}}, "required": ["command", "is_background"]}}
</function>

<function>
{"name": "get_terminal_output", "description": "Get the output of a terminal command previously started with run_in_terminal. The tool will intelligently wait for command completion based on the wait_seconds parameter.\n\nTips:\n- Choose wait_seconds based on command type: 2s for quick commands (ls, cat), 15s for compilation/tests, 30s for application startup.\n- Default wait is 2s.", "parameters": {"type": "object", "properties": {"terminal_id": {"type": "string", "description": "The ID of the terminal command output to check."}, "wait_seconds": {"type": "integer", "description": "Wait time in seconds for command completion. Default is 2."}}, "required": ["terminal_id"]}}
</function>

<function>
{"name": "search_codebase", "description": "Semantic code search that finds code by meaning, not exact text. Use when you don't know which files contain needed info, or to ask high-level 'where/how' questions about the codebase.\n\nExamples: \"authentication logic\", \"how payments work\", \"database connection handling\".\n\nDecision Rule: If query contains PascalCase, camelCase, or 'class/interface/method + Name', use search_symbol instead.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "A concise, keyword-rich search string optimized for code retrieval. Should contain technical terms, relevant programming concepts, and implementation-focused language. Emphasize code functionality, architecture components, and technical specifications."}, "key_words": {"type": "string", "description": "Extract maximum three most important keywords from the query, ranked by relevance. Prioritize keywords most relevant to the query's core intent. Ignore generic programming words. Format: keyword1,keyword2."}, "target_directories": {"type": "array", "items": {"type": "string"}, "description": "Absolute directory paths to narrow the search scope. Each path must be a workspace root or subdirectory. Glob patterns are not supported."}}, "required": ["query", "key_words"]}}
</function>

<function>
{"name": "search_symbol", "description": "Tool for discovering code symbols and their relationships.\n\nWHEN TO USE:\n- For class names (e.g. \"PmsProduct\")\n- For method names (e.g. \"UserService.createUser\", \"createUser\")\n- For any visible symbol names in queries/messages\n- Discover related symbols through relationships\n\nUsage:\n- Accepts array of search objects via \"queries\".\n- Each query needs \"symbol\" (exact name to search).\n- Optional: \"file_path\", \"relation\".\n\nRelation filter options:\n- calls: Symbols called by the current symbol\n- called_by: Symbols that call the current symbol\n- references: Symbols referenced by the current symbol\n- referenced_by: Symbols that reference the current symbol\n- extends: Symbols extended by the current symbol\n- extended_by: Symbols that extend the current symbol\n- implements: Interfaces implemented by the current symbol\n- implemented_by: Symbols that implement the current symbol\n- contains: Symbols contained within the current symbol\n- contained_by: Symbols that contain the current symbol\n- overrides: Methods overridden by the current symbol\n- overridden_by: Symbols that override the current symbol\n- all: All relationships\n- none: Only symbol definitions, no relationships\n\nIMPORTANT: Avoid using 'all' relation when possible. Empty result means no matches found.", "parameters": {"type": "object", "properties": {"queries": {"type": "array", "items": {"type": "object", "properties": {"symbol": {"type": "string", "description": "The symbol name to search for. One symbol per object."}, "relation": {"type": "string", "enum": ["calls", "called_by", "references", "referenced_by", "extends", "extended_by", "implements", "implemented_by", "contains", "contained_by", "overrides", "overridden_by", "all", "none"], "description": "The relationship to retrieve for this symbol."}, "file_path": {"type": "string", "description": "Optional file path to narrow the search."}}}}, "description": "Array of search objects, each describing a single symbol lookup."}, "required": ["queries"]}}
</function>

<function>
{"name": "fetch_content", "description": "Fetches the main content from a web page. The URL must be an HTTP or HTTPS URL pointing to a valid internet resource accessible via web browser.\n\nUseful for summarizing or analyzing the content of a webpage. Use this tool when looking for information from a specific webpage.", "parameters": {"type": "object", "properties": {"url": {"type": "string", "description": "URL to read content from. Must start with http:// or https://."}, "query": {"type": "string", "description": "The query to search for in the web page's content. Should be a clear and concise description of the content you want to find."}}, "required": ["url"]}}
</function>

<function>
{"name": "search_web", "description": "Explore the web for real-time information on any topic.\n\nUse this tool when you need up-to-date information that might not be included in existing knowledge, or when you need to verify current facts. The search results will include relevant snippets and URLs from web pages.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Search query. Value range: 1 to 100 characters."}, "timeRange": {"type": "string", "enum": ["OneDay", "OneWeek", "OneMonth", "OneYear", "NoLimit"], "description": "The time range of the query. Default is NoLimit."}}, "required": ["query"]}}
</function>

<function>
{"name": "get_problems", "description": "Get any compile or lint errors in a code file. If the user mentions errors or problems in a file, they may be referring to these. Use the tool to see the same errors that the user is seeing.", "parameters": {"type": "object", "properties": {"file_paths": {"type": "array", "items": {"type": "string", "description": "The absolute path of a file."}, "description": "The absolute paths of mentioned or edited files. Multiple files supported."}}, "required": ["file_paths"]}}
</function>

<function>
{"name": "todo_write", "description": "Use this tool to create and manage a todo list for tracking complex multi-step tasks.\n\nUse proactively when:\n1. Complex multi-step tasks requiring 3+ distinct steps.\n2. Non-trivial tasks requiring careful planning.\n3. User explicitly requests a todo list.\n4. User provides multiple tasks.\n5. After receiving new instructions to capture requirements.\n\nSkip when:\n1. Single, straightforward task.\n2. Trivial task with no tracking benefit.\n3. Task completable in fewer than 3 trivial steps.\n4. Purely conversational or informational task.\n\nStatus values:\n- PENDING: Not yet started.\n- IN_PROGRESS: Currently working on.\n- COMPLETE: Finished all required outputs.\n- CANCELLED: No longer relevant.", "parameters": {"type": "object", "properties": {"todos": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "description": "Unique identifier for the todo item."}, "content": {"type": "string", "description": "The description/content of the todo item (max 70 characters recommended)."}, "status": {"type": "string", "enum": ["PENDING", "IN_PROGRESS", "COMPLETE", "CANCELLED"], "description": "The status of the todo item."}}}, "description": "Array of todo items (minimum 2 items recommended)."}, "merge": {"type": "boolean", "description": "Whether to merge the todos with existing. true=merge by id, false=replace all."}}, "required": ["merge", "todos"]}}
</function>

<function>
{"name": "TaskGet", "description": "Retrieve a task by its ID from the task list.\n\nWhen to use:\n- When you need the full description and context before starting work on a task.\n- To understand task dependencies (what it blocks, what blocks it).\n- After being assigned a task, to get complete requirements.\n\nOutput returns full task details: subject, description, status ('pending', 'in_progress', or 'completed'), blocks, and blockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to retrieve."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "TaskUpdate", "description": "Update a task in the task list.\n\nWhen to use:\n- Mark tasks as resolved when work is completed.\n- Update task details when requirements change.\n- Establish dependencies between tasks.\n\nIMPORTANT:\n- Always mark assigned tasks as resolved when finished.\n- ONLY mark a task as completed when FULLY accomplished.\n- If encountering errors or blockers, keep as in_progress.\n- Never mark as completed if tests are failing, implementation is partial, or unresolved errors exist.\n\nStatus progresses: pending → in_progress → completed.\n\nUpdatable fields: status, subject, description, activeForm, owner, metadata, addBlocks, addBlockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to update."}, "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled", "failed"], "description": "New status for the task."}, "subject": {"type": "string", "description": "New subject/title for the task (imperative form)."}, "description": {"type": "string", "description": "New description for the task."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress (e.g. 'Running tests')."}, "owner": {"type": "string", "description": "New owner for the task (agent name)."}, "metadata": {"type": "object", "description": "Metadata keys to merge into the task. Set a key to null to delete it."}, "addBlocks": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that this task blocks."}, "addBlockedBy": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that block this task."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "SendMessage", "description": "Send a message to another agent in the team.\n\nUse this tool to communicate with other agents (leader or experts). Messages are stored in the recipient's inbox and can be polled for new messages.\n\nCommon use cases:\n- Notify the leader about task completion.\n- Ask questions to other agents.\n- Share important findings or results.\n- Request help or clarification.\n\nThe recipient will receive the message when they check their inbox.", "parameters": {"type": "object", "properties": {"recipient": {"type": "string", "description": "The name of the recipient agent (e.g. 'leader', expert agent name)."}, "content": {"type": "string", "description": "The main content of the message."}, "summary": {"type": "string", "description": "A brief summary of the message (shown in notifications)."}, "type": {"type": "string", "enum": ["message"], "description": "The type of message. Use 'message' for standard messages."}}, "required": ["recipient", "content"]}}
</function>

<function>
{"name": "update_memory", "description": "Update, add, or delete memories to maintain memory consistency.\n\nWHEN TO USE:\n- When the user explicitly requests it.\n- When the user's intent is to add, delete, or modify a memory.\n\nBefore performing the operation, if the relevant memory does not already exist, use search_memory first to check the current memory status.\n\nMemory categories include:\n- User Preferences: user_info, user_hobby, user_communication\n- Project Information: project_tech_stack, project_build_configuration, project_dependency_configuration, project_ide_configuration, project_scm_configuration, project_environment_configuration, project_introduction\n- Development Standards: development_code_specification, development_practice_specification, development_test_specification, development_comment_specification\n- Lessons Learned: expert_experience, learned_skill_experience", "parameters": {"type": "object", "properties": {"title": {"type": "string", "description": "Descriptive title for a new or updated memory. Required when creating or updating."}, "content": {"type": "string", "description": "Content of a new or updated memory. Leave blank when deleting."}, "keywords": {"type": "string", "description": "Keywords to associate with the memory, comma-separated. Used for filtering and retrieval. Only used when creating."}, "category": {"type": "string", "description": "Memory category. Must be one of the predefined categories."}, "action": {"type": "string", "description": "The type of action: 'create', 'update', or 'delete'."}, "id": {"type": "string", "description": "ID of an existing memory to update or delete. Leave blank when creating."}, "scope": {"type": "string", "description": "Set to 'global' (cross-project) or 'workspace' (current project only). Only set when user explicitly requests."}}, "required": ["title", "content", "keywords", "category", "action"]}}
</function>

<function>
{"name": "search_memory", "description": "Retrieve detailed memories from the memory system.\n\nWHEN TO USE:\n- At any task node to recall relevant memories for efficient resolution.\n- At task start, retrieve background information and past experiences.\n- When using other retrieval tools, recall more relevant details.\n- If a tool execution fails, recall potential solutions.\n\nSearch approach:\n- First perform a shallow search for relevant keywords and categories.\n- If task is not completed after shallow search, perform a deep search.\n- When user explicitly requests recall or expresses doubt, use deep mode.\n\nDepth levels:\n- 'shallow': Smaller search scope, fewer but highly relevant memories. Use when task information is sufficient.\n- 'deep': Larger search scope, more comprehensive memories. Use when more information is needed or memory overview shows few relevant memories.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "What you want to recall. Keep it semantically clear, complete, and concise."}, "keywords": {"type": "string", "description": "Keywords describing the memory. Up to 5 keywords, each max 10 characters, comma-separated."}, "category": {"type": "string", "description": "Categories to search. Comma-separated. Use empty string to search all categories."}, "depth": {"type": "string", "description": "Recall depth: 'shallow' for focused results, 'deep' for comprehensive retrieval."}}, "required": ["query", "keywords", "category", "depth"]}}
</function>

</functions>
---
name: backend-dev-tools
description: backend-dev角色的完整工具定义
agent: backend-dev
tool_count: 20
---

<functions>

<function>
{"name": "read_file", "description": "Reads a file from the local filesystem. You can access any file directly by using this tool.\n\nUsage:\n- Provide the absolute path to the target file.\n- You can read the entire file by providing just the file path, which is recommended for better understanding and accuracy.\n- For very large files or when you need specific sections, you can optionally specify line ranges using start_line and end_line.\n- Supports reading image files (jpeg/jpg, png, webp).\n- If the file does not exist, an error will be returned.\n\nBest Practices:\n- Always ensure you have the COMPLETE context before making decisions.\n- If the retrieved range is insufficient, call this tool again to fetch additional content.\n- Use line ranges only when the file is too large to read at once.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the file to read."}, "start_line": {"type": "integer", "description": "The line number to start reading from, 1-based. Only provide if the file is too large to read at once."}, "end_line": {"type": "integer", "description": "The inclusive line number to end reading at, 1-based. Only provide if the file is too large to read at once."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "search_replace", "description": "Performs exact string replacements in files.\n\nUsage:\n- When editing text, ensure you preserve the exact indentation (tabs/spaces) as it appears before.\n- ALWAYS prefer editing existing files in the codebase. NEVER write new files unless explicitly required.\n- The edit will FAIL if old_string is not unique in the file. Either provide a larger string with more surrounding context to make it unique, or use replace_all to change every instance of old_string.\n- Use replace_all for replacing and renaming strings across the file. This parameter is useful if you want to rename a variable, for instance.\n- To create or overwrite a file, you should prefer the create_file tool.\n\nIMPORTANT:\n- old_string and new_string must be different.\n- Only use emojis if the user explicitly requests it.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The path to the file to modify. You can use either a relative path in the workspace or an absolute path."}, "old_string": {"type": "string", "description": "The text to replace. Must be an exact match of existing content in the file."}, "new_string": {"type": "string", "description": "The text to replace it with. Must be different from old_string."}, "replace_all": {"type": "boolean", "description": "If true, replace all occurrences of old_string in the file. Default is false."}}, "required": ["file_path", "old_string", "new_string"]}}
</function>

<function>
{"name": "create_file", "description": "Use this tool to create a new file with content or modify existing files by overwriting.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): Absolute path to the target file.\n- file_content (REQUIRED): The content of the file.\n- add_last_line_newline (OPTIONAL): Whether to add a newline at the end (default: true).\n\nIMPORTANT:\n- MUST LIMIT THE FILE CONTENT TO AT MOST 1000 LINES.\n- When editing existing files, MUST use search_replace tool FIRST, EXCEPT for COMPLETE file modification or overwrite.\n- ALWAYS prefer editing existing files in the codebase.\n- NEVER write new files unless explicitly required.\n- NEVER proactively create documentation files (*.md) or README files unless explicitly requested.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "Absolute path to the target file. Must be generated first before any other arguments."}, "file_content": {"type": "string", "description": "The content of the file."}, "add_last_line_newline": {"type": "boolean", "description": "Whether to add a newline at the end of the file. Default is true."}}, "required": ["file_path", "file_content"]}}
</function>

<function>
{"name": "delete_file", "description": "Delete a file safely. ONLY use this tool to delete files.\n\nCRITICAL REQUIREMENTS:\n- file_path (REQUIRED): The absolute path to the file to delete.\n\nIMPORTANT:\n- This is the only safe tool to delete files.\n- DO NOT use shell commands or launch-process to remove files.", "parameters": {"type": "object", "properties": {"file_path": {"type": "string", "description": "The absolute path to the file to delete."}}, "required": ["file_path"]}}
</function>

<function>
{"name": "grep_code", "description": "High-performance file content search utilizing the ripgrep engine.\n\nGuidelines:\n- Recommended for precise string matching. Faster than terminal grep/rg and honors .gitignore automatically.\n- Full regex support available, such as \"error.*Handler\", \"class\\\\s+\\\\w+\". Special characters require escaping for exact matches, e.g. \"getUser\\\\(\".\n- Pattern format follows ripgrep conventions (not standard grep). Literal braces require escaping (e.g. map\\\\[string\\\\] to match map[string] in Go).\n- Broad glob patterns like '--glob *' should be avoided as they circumvent .gitignore and reduce performance.\n- Apply 'type' or 'glob' parameters only when file type is certain.\n- Results auto-expand to complete syntax blocks (functions, methods, structs) for better context.\n- Line matching: Single-line by default. Cross-line patterns require multiline: true flag.", "parameters": {"type": "object", "properties": {"regex": {"type": "string", "description": "Regex pattern for searching within file contents (rg --regexp)."}, "path": {"type": "string", "description": "Target file or directory for search (rg PATTERN [PATH ...]). Uses workspace roots by default."}, "type": {"type": "string", "description": "Specific file type for search (rg --type). Common options: js, py, rust, go, java, etc. Preferred over glob for standard types."}, "glob": {"type": "string", "description": "File filtering glob pattern (rg --glob GLOB -- PATH), such as \"*.java\" or \"*.{js,ts,tsx}\"."}, "multiline": {"type": "boolean", "description": "Activate multiline matching where . includes newlines and patterns cross line boundaries (rg -U --multiline-dotall). Default: false."}, "-A": {"type": "number", "description": "Lines of context to display after match (rg -A)."}, "-B": {"type": "number", "description": "Lines of context to display before match (rg -B)."}, "-C": {"type": "number", "description": "Lines of context to display both before and after match (rg -C)."}, "-i": {"type": "boolean", "description": "Perform case-insensitive search (rg -i). Default: false."}}, "required": ["regex"]}}
</function>

<function>
{"name": "search_file", "description": "Search for files by glob pattern (such as *.go or config/*.json) in workspace.\nONLY supports glob patterns, NOT regex. This only returns the paths of matching files. Limited to 25 results.\nMake your query more specific if you need to filter results further.\n\nExamples:\n- \"*.go\" (converted to \"**/*.go\") - matches all .go files\n- \"**/src/**\" - matches all files under src directories\n- \"**/test/**/test_*.py\" - matches test_*.py files within any test directory\n\nIf the pattern does not begin with \"**/\", it will be automatically prefixed with \"**/\" for recursive search.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Glob pattern for matching files."}, "path": {"type": "string", "description": "Directory path to search within. Supports both absolute paths and relative paths (resolved from workspace root). When omitted, searches from the workspace root."}}, "required": ["query"]}}
</function>

<function>
{"name": "list_dir", "description": "List the contents of a directory. Useful to understand the file structure before diving deeper into specific files.\n\nIMPORTANT:\n- The path parameter must be an absolute path.\n- Unless requested by the user, do not recursively check directories layer by layer; try to lock the directory location first before viewing.", "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Absolute path to the directory to list contents of."}}, "required": ["path"]}}
</function>

<function>
{"name": "run_in_terminal", "description": "Execute a shell command in the terminal.\n\nIMPORTANT: This tool is for terminal operations like git, npm, docker, etc. DO NOT use it for file operations (reading, writing, editing, searching, finding files) - use the specialized tools instead.\n\nRules:\n1. Ensure commands are compatible with the user's operating system.\n2. Working directory inherits from previous step if shell reuse is possible; otherwise uses workspace root. Use absolute paths and avoid cd.\n3. For long-running commands or those expected to keep running, set is_background to true. Use get_terminal_output to check background process output later.\n4. For commands requiring user interaction, pass non-interactive flags (e.g. --yes).\n5. Do not delete directories or files through commands.\n6. Do not use commands that may harm the user's OS (e.g. rm, sudo, wget, curl).\n7. Do not use newline characters in the command.\n8. Commands must not exceed 500 characters. Write longer commands to a file first.\n9. Assess risk before execution. Read-only commands are generally safe; destructive commands (rm, wget) are risky.\n10. NEVER update git config or run destructive/irreversible git commands unless explicitly requested.\n11. NEVER commit changes unless explicitly asked.", "parameters": {"type": "object", "properties": {"command": {"type": "string", "description": "REQUIRED. The command to run in the terminal."}, "is_background": {"type": "boolean", "description": "REQUIRED. Whether the command starts a background process. If true, runs in background and returns a terminal ID for later output checking."}, "timeout": {"type": "integer", "description": "Optional. Timeout in milliseconds for command completion (default 180000, max 1800000). Foreground commands exceeding timeout are auto-converted to background."}, "has_risk": {"type": "boolean", "description": "Whether the command is potentially risky (e.g. destructive operations)."}, "required_permissions": {"type": "string", "description": "Optional. Set to 'all' to request running outside sandbox. Only use when confirmed failure was caused by sandbox restrictions."}, "command_names": {"type": "array", "items": {"type": "string"}, "description": "Optional. Parsed command names from the command (e.g. [\"cd\", \"ls\"])."}}, "required": ["command", "is_background"]}}
</function>

<function>
{"name": "get_terminal_output", "description": "Get the output of a terminal command previously started with run_in_terminal. The tool will intelligently wait for command completion based on the wait_seconds parameter.\n\nTips:\n- Choose wait_seconds based on command type: 2s for quick commands (ls, cat), 15s for compilation/tests, 30s for application startup.\n- Default wait is 2s.", "parameters": {"type": "object", "properties": {"terminal_id": {"type": "string", "description": "The ID of the terminal command output to check."}, "wait_seconds": {"type": "integer", "description": "Wait time in seconds for command completion. Default is 2."}}, "required": ["terminal_id"]}}
</function>

<function>
{"name": "search_codebase", "description": "Semantic code search that finds code by meaning, not exact text. Use when you don't know which files contain needed info, or to ask high-level 'where/how' questions about the codebase.\n\nExamples: \"authentication logic\", \"how payments work\", \"database connection handling\".\n\nDecision Rule: If query contains PascalCase, camelCase, or 'class/interface/method + Name', use search_symbol instead.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "A concise, keyword-rich search string optimized for code retrieval. Should contain technical terms, relevant programming concepts, and implementation-focused language. Emphasize code functionality, architecture components, and technical specifications."}, "key_words": {"type": "string", "description": "Extract maximum three most important keywords from the query, ranked by relevance. Prioritize keywords most relevant to the query's core intent. Ignore generic programming words. Format: keyword1,keyword2."}, "target_directories": {"type": "array", "items": {"type": "string"}, "description": "Absolute directory paths to narrow the search scope. Each path must be a workspace root or subdirectory. Glob patterns are not supported."}}, "required": ["query", "key_words"]}}
</function>

<function>
{"name": "search_symbol", "description": "Tool for discovering code symbols and their relationships.\n\nWHEN TO USE:\n- For class names (e.g. \"PmsProduct\")\n- For method names (e.g. \"UserService.createUser\", \"createUser\")\n- For any visible symbol names in queries/messages\n- Discover related symbols through relationships\n\nUsage:\n- Accepts array of search objects via \"queries\".\n- Each query needs \"symbol\" (exact name to search).\n- Optional: \"file_path\", \"relation\".\n\nRelation filter options:\n- calls: Symbols called by the current symbol\n- called_by: Symbols that call the current symbol\n- references: Symbols referenced by the current symbol\n- referenced_by: Symbols that reference the current symbol\n- extends: Symbols extended by the current symbol\n- extended_by: Symbols that extend the current symbol\n- implements: Interfaces implemented by the current symbol\n- implemented_by: Symbols that implement the current symbol\n- contains: Symbols contained within the current symbol\n- contained_by: Symbols that contain the current symbol\n- overrides: Methods overridden by the current symbol\n- overridden_by: Symbols that override the current symbol\n- all: All relationships\n- none: Only symbol definitions, no relationships\n\nIMPORTANT: Avoid using 'all' relation when possible. Empty result means no matches found.", "parameters": {"type": "object", "properties": {"queries": {"type": "array", "items": {"type": "object", "properties": {"symbol": {"type": "string", "description": "The symbol name to search for. One symbol per object."}, "relation": {"type": "string", "enum": ["calls", "called_by", "references", "referenced_by", "extends", "extended_by", "implements", "implemented_by", "contains", "contained_by", "overrides", "overridden_by", "all", "none"], "description": "The relationship to retrieve for this symbol."}, "file_path": {"type": "string", "description": "Optional file path to narrow the search."}}}}, "description": "Array of search objects, each describing a single symbol lookup."}, "required": ["queries"]}}
</function>

<function>
{"name": "fetch_content", "description": "Fetches the main content from a web page. The URL must be an HTTP or HTTPS URL pointing to a valid internet resource accessible via web browser.\n\nUseful for summarizing or analyzing the content of a webpage. Use this tool when looking for information from a specific webpage.", "parameters": {"type": "object", "properties": {"url": {"type": "string", "description": "URL to read content from. Must start with http:// or https://."}, "query": {"type": "string", "description": "The query to search for in the web page's content. Should be a clear and concise description of the content you want to find."}}, "required": ["url"]}}
</function>

<function>
{"name": "search_web", "description": "Explore the web for real-time information on any topic.\n\nUse this tool when you need up-to-date information that might not be included in existing knowledge, or when you need to verify current facts. The search results will include relevant snippets and URLs from web pages.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Search query. Value range: 1 to 100 characters."}, "timeRange": {"type": "string", "enum": ["OneDay", "OneWeek", "OneMonth", "OneYear", "NoLimit"], "description": "The time range of the query. Default is NoLimit."}}, "required": ["query"]}}
</function>

<function>
{"name": "get_problems", "description": "Get any compile or lint errors in a code file. If the user mentions errors or problems in a file, they may be referring to these. Use the tool to see the same errors that the user is seeing.", "parameters": {"type": "object", "properties": {"file_paths": {"type": "array", "items": {"type": "string", "description": "The absolute path of a file."}, "description": "The absolute paths of mentioned or edited files. Multiple files supported."}}, "required": ["file_paths"]}}
</function>

<function>
{"name": "todo_write", "description": "Use this tool to create and manage a todo list for tracking complex multi-step tasks.\n\nUse proactively when:\n1. Complex multi-step tasks requiring 3+ distinct steps.\n2. Non-trivial tasks requiring careful planning.\n3. User explicitly requests a todo list.\n4. User provides multiple tasks.\n5. After receiving new instructions to capture requirements.\n\nSkip when:\n1. Single, straightforward task.\n2. Trivial task with no tracking benefit.\n3. Task completable in fewer than 3 trivial steps.\n4. Purely conversational or informational task.\n\nStatus values:\n- PENDING: Not yet started.\n- IN_PROGRESS: Currently working on.\n- COMPLETE: Finished all required outputs.\n- CANCELLED: No longer relevant.", "parameters": {"type": "object", "properties": {"todos": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "description": "Unique identifier for the todo item."}, "content": {"type": "string", "description": "The description/content of the todo item (max 70 characters recommended)."}, "status": {"type": "string", "enum": ["PENDING", "IN_PROGRESS", "COMPLETE", "CANCELLED"], "description": "The status of the todo item."}}}, "description": "Array of todo items (minimum 2 items recommended)."}, "merge": {"type": "boolean", "description": "Whether to merge the todos with existing. true=merge by id, false=replace all."}}, "required": ["merge", "todos"]}}
</function>

<function>
{"name": "TaskGet", "description": "Retrieve a task by its ID from the task list.\n\nWhen to use:\n- When you need the full description and context before starting work on a task.\n- To understand task dependencies (what it blocks, what blocks it).\n- After being assigned a task, to get complete requirements.\n\nOutput returns full task details: subject, description, status ('pending', 'in_progress', or 'completed'), blocks, and blockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to retrieve."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "TaskUpdate", "description": "Update a task in the task list.\n\nWhen to use:\n- Mark tasks as resolved when work is completed.\n- Update task details when requirements change.\n- Establish dependencies between tasks.\n\nIMPORTANT:\n- Always mark assigned tasks as resolved when finished.\n- ONLY mark a task as completed when FULLY accomplished.\n- If encountering errors or blockers, keep as in_progress.\n- Never mark as completed if tests are failing, implementation is partial, or unresolved errors exist.\n\nStatus progresses: pending → in_progress → completed.\n\nUpdatable fields: status, subject, description, activeForm, owner, metadata, addBlocks, addBlockedBy.", "parameters": {"type": "object", "properties": {"taskId": {"type": "string", "description": "The ID of the task to update."}, "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled", "failed"], "description": "New status for the task."}, "subject": {"type": "string", "description": "New subject/title for the task (imperative form)."}, "description": {"type": "string", "description": "New description for the task."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress (e.g. 'Running tests')."}, "owner": {"type": "string", "description": "New owner for the task (agent name)."}, "metadata": {"type": "object", "description": "Metadata keys to merge into the task. Set a key to null to delete it."}, "addBlocks": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that this task blocks."}, "addBlockedBy": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that block this task."}}, "required": ["taskId"]}}
</function>

<function>
{"name": "SendMessage", "description": "Send a message to another agent in the team.\n\nUse this tool to communicate with other agents (leader or experts). Messages are stored in the recipient's inbox and can be polled for new messages.\n\nCommon use cases:\n- Notify the leader about task completion.\n- Ask questions to other agents.\n- Share important findings or results.\n- Request help or clarification.\n\nThe recipient will receive the message when they check their inbox.", "parameters": {"type": "object", "properties": {"recipient": {"type": "string", "description": "The name of the recipient agent (e.g. 'leader', expert agent name)."}, "content": {"type": "string", "description": "The main content of the message."}, "summary": {"type": "string", "description": "A brief summary of the message (shown in notifications)."}, "type": {"type": "string", "enum": ["message"], "description": "The type of message. Use 'message' for standard messages."}}, "required": ["recipient", "content"]}}
</function>

<function>
{"name": "update_memory", "description": "Update, add, or delete memories to maintain memory consistency.\n\nWHEN TO USE:\n- When the user explicitly requests it.\n- When the user's intent is to add, delete, or modify a memory.\n\nBefore performing the operation, if the relevant memory does not already exist, use search_memory first to check the current memory status.\n\nMemory categories include:\n- User Preferences: user_info, user_hobby, user_communication\n- Project Information: project_tech_stack, project_build_configuration, project_dependency_configuration, project_ide_configuration, project_scm_configuration, project_environment_configuration, project_introduction\n- Development Standards: development_code_specification, development_practice_specification, development_test_specification, development_comment_specification\n- Lessons Learned: expert_experience, learned_skill_experience", "parameters": {"type": "object", "properties": {"title": {"type": "string", "description": "Descriptive title for a new or updated memory. Required when creating or updating."}, "content": {"type": "string", "description": "Content of a new or updated memory. Leave blank when deleting."}, "keywords": {"type": "string", "description": "Keywords to associate with the memory, comma-separated. Used for filtering and retrieval. Only used when creating."}, "category": {"type": "string", "description": "Memory category. Must be one of the predefined categories."}, "action": {"type": "string", "description": "The type of action: 'create', 'update', or 'delete'."}, "id": {"type": "string", "description": "ID of an existing memory to update or delete. Leave blank when creating."}, "scope": {"type": "string", "description": "Set to 'global' (cross-project) or 'workspace' (current project only). Only set when user explicitly requests."}}, "required": ["title", "content", "keywords", "category", "action"]}}
</function>

<function>
{"name": "search_memory", "description": "Retrieve detailed memories from the memory system.\n\nWHEN TO USE:\n- At any task node to recall relevant memories for efficient resolution.\n- At task start, retrieve background information and past experiences.\n- When using other retrieval tools, recall more relevant details.\n- If a tool execution fails, recall potential solutions.\n\nSearch approach:\n- First perform a shallow search for relevant keywords and categories.\n- If task is not completed after shallow search, perform a deep search.\n- When user explicitly requests recall or expresses doubt, use deep mode.\n\nDepth levels:\n- 'shallow': Smaller search scope, fewer but highly relevant memories. Use when task information is sufficient.\n- 'deep': Larger search scope, more comprehensive memories. Use when more information is needed or memory overview shows few relevant memories.", "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "What you want to recall. Keep it semantically clear, complete, and concise."}, "keywords": {"type": "string", "description": "Keywords describing the memory. Up to 5 keywords, each max 10 characters, comma-separated."}, "category": {"type": "string", "description": "Categories to search. Comma-separated. Use empty string to search all categories."}, "depth": {"type": "string", "description": "Recall depth: 'shallow' for focused results, 'deep' for comprehensive retrieval."}}, "required": ["query", "keywords", "category", "depth"]}}
</function>

</functions>
---
name: verify-expert-tools
agent: verify-expert
tool_count: 20
---

<functions>
<function>
{"name": "read_file", "description": "Reads a file from the local filesystem. You can access any file directly by using this tool. Supports reading entire files or specific line ranges. Also supports reading image files (jpeg/jpg, png, webp).\n\nUsage:\n- Provide just the file path to read the entire file.\n- Optionally specify start_line and end_line to read a specific range of lines.\n- For very large files, use line ranges to read in chunks.\n- Can also read image files when called with the appropriate path.", "parameters": {"type": "object", "required": ["file_path"], "properties": {"file_path": {"type": "string", "description": "Absolute path to the file to read."}, "start_line": {"type": "integer", "description": "The line number to start reading from, 1-based. Only provide if the file is too large to read at once."}, "end_line": {"type": "integer", "description": "The inclusive line number to end reading at, 1-based. Only provide if the file is too large to read at once."}}}}
</function>
<function>
{"name": "grep_code", "description": "High-performance file content search utilizing the ripgrep engine. Supports full regex, automatic .gitignore honoring, and auto-expansion of matched results to complete syntax blocks (functions, methods, structs) for better context.\n\nGuidelines:\n- Recommended for precise string matching.\n- Full regex support (e.g. \"error.*Handler\", \"class\\\\s+\\\\w+\"). Special characters require escaping for exact matches.\n- Pattern format follows ripgrep conventions.\n- Apply 'type' or 'glob' parameters only when file type is certain.\n- Results auto-expand to complete syntax blocks for better context.\n- Single-line matching by default. Cross-line patterns require multiline: true.", "parameters": {"type": "object", "required": ["regex"], "properties": {"regex": {"type": "string", "description": "Regex pattern for searching within file contents (rg --regexp)."}, "path": {"type": "string", "description": "Target file or directory for search. Uses workspace roots by default."}, "type": {"type": "string", "description": "Specific file type for search (rg --type). Common options: js, py, rust, go, java, etc."}, "glob": {"type": "string", "description": "File filtering glob pattern (rg --glob GLOB), such as \"*.java\" or \"*.{js,ts,tsx}\"."}, "multiline": {"type": "boolean", "description": "Activate multiline matching where . includes newlines and patterns cross line boundaries. Default: false."}, "-A": {"type": "number", "description": "Lines of context to display after match (rg -A)."}, "-B": {"type": "number", "description": "Lines of context to display before match (rg -B)."}, "-C": {"type": "number", "description": "Lines of context to display both before and after match (rg -C)."}, "-i": {"type": "boolean", "description": "Perform case-insensitive search (rg -i). Defaults to false."}}}}
</function>
<function>
{"name": "search_codebase", "description": "Semantic code search that finds code by meaning, not exact text. Use when you need to understand how something works, where functionality is implemented, or explore unfamiliar codebases. Finds relevant code across the entire project based on natural language queries.\n\nExamples: \"authentication logic\", \"how payments work\", \"database connection handling\".", "parameters": {"type": "object", "required": ["query", "key_words"], "properties": {"query": {"type": "string", "description": "A concise, keyword-rich search string optimized for code retrieval. Should contain technical terms, relevant programming concepts, and implementation-focused language."}, "key_words": {"type": "string", "description": "Extract maximum three most important keywords from the query, ranked by relevance. Format: keyword1,keyword2,keyword3"}, "target_directories": {"type": "array", "items": {"type": "string"}, "description": "Absolute directory paths to narrow the search scope."}}}}
</function>
<function>
{"name": "search_symbol", "description": "Tool for discovering code symbols and their relationships. Use for class names, method names, variable names, or any visible symbol names. Supports relationship queries such as calls, called_by, references, referenced_by, extends, extended_by, implements, implemented_by, contains, contained_by, overrides, overridden_by, all, none.", "parameters": {"type": "object", "required": ["queries"], "properties": {"queries": {"type": "array", "items": {"type": "object", "properties": {"symbol": {"type": "string", "description": "The symbol name to search for. One symbol per object."}, "file_path": {"type": "string", "description": "Optional file path to narrow the search."}, "relation": {"type": "string", "enum": ["calls", "called_by", "references", "referenced_by", "extends", "extended_by", "implements", "implemented_by", "contains", "contained_by", "overrides", "overridden_by", "all", "none"], "description": "The relationship to retrieve for this symbol."}}}, "description": "Array of search objects describing symbol lookups."}}}}
</function>
<function>
{"name": "search_file", "description": "Search for files by glob pattern (such as *.go or config/*.json) in workspace. Only supports glob patterns, NOT regex. Returns the paths of matching files. Limited to 25 results.\n\nExamples:\n- \"*.go\" (converted to \"**/*.go\") - matches all .go files\n- \"**/src/**\" - matches all files under src directories\n- \"**/test/**/test_*.py\" - matches test_*.py files within any test directory", "parameters": {"type": "object", "required": ["query"], "properties": {"query": {"type": "string", "description": "Glob pattern for matching files. If the pattern does not begin with \"**/\", it will be automatically prefixed with \"**/\" for recursive search."}, "path": {"type": "string", "description": "Directory path to search within. Supports both absolute and relative paths."}}}}
</function>
<function>
{"name": "list_dir", "description": "List the contents of a directory. Useful to understand the file structure before diving deeper into specific files. The path parameter must be an absolute path.", "parameters": {"type": "object", "required": ["path"], "properties": {"path": {"type": "string", "description": "Absolute path to list contents of."}}}}
</function>
<function>
{"name": "run_in_terminal", "description": "Execute a shell command in the terminal. Use for terminal operations like git, npm, docker, build commands, test runners, etc.\n\nRules:\n- For long-running or persistent processes, set is_background to true.\n- Background terminals return a terminal_id for later output checking via get_terminal_output.\n- Use absolute paths and avoid cd when possible.\n- Do not use newline characters in the command.\n- Commands must not exceed 500 characters.", "parameters": {"type": "object", "required": ["command", "is_background"], "properties": {"command": {"type": "string", "description": "The command to run in the terminal."}, "is_background": {"type": "boolean", "description": "Whether the command starts a background process. If true, the command runs in the background and returns a terminal_id."}, "timeout": {"type": "integer", "description": "Timeout in milliseconds for the command to complete (default 180000, max 1800000)."}, "has_risk": {"type": "boolean", "description": "Whether the command is potentially risky."}, "required_permissions": {"type": "string", "description": "Set to 'all' to request running outside the sandbox."}, "command_names": {"type": "array", "items": {"type": "string"}, "description": "Parsed command names from the command."}}}}
</function>
<function>
{"name": "get_terminal_output", "description": "Get the output of a terminal command previously started with run_in_terminal. The tool will intelligently wait for command completion based on the wait_seconds parameter.", "parameters": {"type": "object", "required": ["terminal_id"], "properties": {"terminal_id": {"type": "string", "description": "The ID of the terminal command output to check."}, "wait_seconds": {"type": "integer", "description": "Wait time in seconds for command completion. Choose based on command type: 2s for quick commands, 15s for compilation/tests, 30s for app startup. Default is 2s."}}}}
</function>
<function>
{"name": "create_file", "description": "Create a new file with content or overwrite an existing file. Use this tool to write new files or completely replace existing file contents.\n\nRequirements:\n- file_path must be an absolute path.\n- File content must not exceed 1000 lines.\n- Always prefer editing existing files over creating new ones.\n- Never proactively create documentation files unless explicitly requested.", "parameters": {"type": "object", "required": ["file_path", "file_content"], "properties": {"file_path": {"type": "string", "description": "Absolute path to the target file."}, "file_content": {"type": "string", "description": "The content of the file."}, "add_last_line_newline": {"type": "boolean", "description": "Whether to add a newline at the end of the file. Default: true."}}}}
</function>
<function>
{"name": "delete_file", "description": "Delete a file safely. This is the only safe tool to delete files. Do not use shell commands to remove files.\n\nRequirements:\n- file_path must be an absolute path.", "parameters": {"type": "object", "required": ["file_path"], "properties": {"file_path": {"type": "string", "description": "The absolute file path to delete."}}}}
</function>
<function>
{"name": "search_replace", "description": "Performs exact string replacements in files. Use for editing existing files by replacing specific text with new text.\n\nRules:\n- Preserve exact indentation (tabs/spaces) as it appears in the original.\n- The edit will FAIL if old_string is not unique in the file. Provide more surrounding context to make it unique, or use replace_all.\n- Use replace_all for renaming variables or replacing all occurrences.", "parameters": {"type": "object", "required": ["file_path", "old_string", "new_string"], "properties": {"file_path": {"type": "string", "description": "The path to the file to modify."}, "old_string": {"type": "string", "description": "The text to replace. Must be unique in the file unless replace_all is true."}, "new_string": {"type": "string", "description": "The text to replace it with (must be different from old_string)."}, "replace_all": {"type": "boolean", "description": "Replace all occurrences of old_string. Default: false."}}}}
</function>
<function>
{"name": "fetch_content", "description": "Fetches the main content from a web page. Useful for summarizing or analyzing the content of a webpage. The URL must be an HTTP or HTTPS URL pointing to a valid internet resource.", "parameters": {"type": "object", "required": ["url"], "properties": {"url": {"type": "string", "description": "URL to read content from. Must start with http:// or https://."}, "query": {"type": "string", "description": "Optional query to search for in the web page's content. Should be a clear and concise description of the content you want to find."}}}}
</function>
<function>
{"name": "search_web", "description": "Explore the web for real-time information on any topic. Use when you need up-to-date information that might not be in existing knowledge, or when you need to verify current facts. Returns relevant snippets and URLs from web pages.", "parameters": {"type": "object", "required": ["query"], "properties": {"query": {"type": "string", "description": "Search query. Value range: 1 to 100 characters."}, "timeRange": {"type": "string", "enum": ["OneDay", "OneWeek", "OneMonth", "OneYear", "NoLimit"], "description": "The time range of the query. Default is NoLimit."}}}}
</function>
<function>
{"name": "get_problems", "description": "Get any compile or lint errors in a code file. If the user mentions errors or problems in a file, they may be referring to these. Use this tool to see the same errors the user is seeing.", "parameters": {"type": "object", "required": ["file_paths"], "properties": {"file_paths": {"type": "array", "items": {"type": "string", "description": "The absolute path of a file to check."}, "description": "The absolute paths of mentioned or edited files."}}}}
</function>
<function>
{"name": "SendMessage", "description": "Send a message to another agent in the team. Use to communicate with other agents (leader or experts) — notify about task completion, ask questions, share findings, or request clarification.", "parameters": {"type": "object", "required": ["recipient", "content"], "properties": {"recipient": {"type": "string", "description": "The name of the recipient agent (e.g., 'leader', expert agent name)."}, "content": {"type": "string", "description": "The main content of the message."}, "summary": {"type": "string", "description": "A brief summary of the message (shown in notifications)."}, "type": {"type": "string", "enum": ["message"], "description": "The type of message. Use 'message' for standard messages."}}}}
</function>
<function>
{"name": "todo_write", "description": "Create and manage a todo list for tracking complex multi-step tasks. Use proactively for complex tasks (3+ steps), non-trivial tasks, user-requested lists, or multiple tasks.\n\nStatus values: PENDING, IN_PROGRESS, COMPLETE, CANCELLED.", "parameters": {"type": "object", "required": ["merge", "todos"], "properties": {"merge": {"type": "boolean", "description": "Whether to merge the todos with existing. true=merge by id, false=replace all."}, "todos": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "description": "Unique identifier for the todo item."}, "content": {"type": "string", "description": "The description/content of the todo item."}, "status": {"type": "string", "enum": ["PENDING", "IN_PROGRESS", "COMPLETE", "CANCELLED"], "description": "The status of the todo item."}}, "required": ["id", "content", "status"]}, "description": "Array of todo items (minimum 2 items recommended)."}}}}
</function>
<function>
{"name": "TaskGet", "description": "Retrieve a task by its ID from the task list. Returns full task details including subject, description, status, blocks, and blockedBy.", "parameters": {"type": "object", "required": ["taskId"], "properties": {"taskId": {"type": "string", "description": "The ID of the task to retrieve."}}}}
</function>
<function>
{"name": "TaskUpdate", "description": "Update a task in the task list. Use to mark tasks as resolved, update details, or establish dependencies.\n\nUpdatable fields: status, subject, description, activeForm, owner, metadata, addBlocks, addBlockedBy.\n\nStatus progresses: pending → in_progress → completed.", "parameters": {"type": "object", "required": ["taskId"], "properties": {"taskId": {"type": "string", "description": "The ID of the task to update."}, "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled", "failed"], "description": "New status for the task."}, "subject": {"type": "string", "description": "New subject for the task (imperative form)."}, "description": {"type": "string", "description": "New description for the task."}, "owner": {"type": "string", "description": "New owner for the task (agent name)."}, "addBlocks": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that this task blocks."}, "addBlockedBy": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that block this task."}, "metadata": {"type": "object", "description": "Metadata keys to merge into the task."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress."}}}}
</function>
<function>
{"name": "update_memory", "description": "Update, add, or delete memories based on the user's intent to maintain memory consistency. Supports creating, updating, and deleting memories across various categories.\n\nMemory categories include: user_info, user_hobby, user_communication, project_tech_stack, project_build_configuration, project_dependency_configuration, project_ide_configuration, project_scm_configuration, project_environment_configuration, development_code_specification, development_practice_specification, development_test_specification, development_comment_specification, expert_experience, learned_skill_experience.", "parameters": {"type": "object", "required": ["title", "content", "keywords", "category", "action"], "properties": {"title": {"type": "string", "description": "Descriptive title for the memory."}, "content": {"type": "string", "description": "Content of the memory."}, "keywords": {"type": "string", "description": "Keywords associated with the memory, comma-separated."}, "category": {"type": "string", "description": "Memory category (e.g., user_info, project_tech_stack, expert_experience, etc.)."}, "action": {"type": "string", "description": "Action type: 'create', 'update', or 'delete'."}, "id": {"type": "string", "description": "ID of an existing memory to update or delete."}, "scope": {"type": "string", "description": "Memory scope: 'global' (cross-project) or 'workspace' (current project only)."}}}}
</function>
<function>
{"name": "search_memory", "description": "Retrieve detailed memories from the memory system. Use to recall relevant information, background context, past experiences, and known solutions.\n\nDepth levels:\n- 'shallow': Smaller scope, fewer but highly relevant results.\n- 'deep': Larger scope, more comprehensive results.", "parameters": {"type": "object", "required": ["query", "keywords", "category", "depth"], "properties": {"query": {"type": "string", "description": "What you want to recall. Keep it semantically clear, complete, and concise."}, "keywords": {"type": "string", "description": "Up to 5 keywords, comma-separated. Each keyword should not exceed 10 characters."}, "category": {"type": "string", "description": "Memory categories to search, comma-separated. Use empty string to search all categories."}, "depth": {"type": "string", "description": "Recall depth: 'shallow' for focused results, 'deep' for comprehensive retrieval."}}}}
</function>
</functions>

---
name: researcher-tools
agent: researcher
tool_count: 20
---

<functions>
<function>
{"name": "read_file", "description": "Reads a file from the local filesystem. You can access any file directly by using this tool. Supports reading entire files or specific line ranges. Also supports reading image files (jpeg/jpg, png, webp).\n\nUsage:\n- Provide just the file path to read the entire file.\n- Optionally specify start_line and end_line to read a specific range of lines.\n- For very large files, use line ranges to read in chunks.\n- Can also read image files when called with the appropriate path.", "parameters": {"type": "object", "required": ["file_path"], "properties": {"file_path": {"type": "string", "description": "Absolute path to the file to read."}, "start_line": {"type": "integer", "description": "The line number to start reading from, 1-based. Only provide if the file is too large to read at once."}, "end_line": {"type": "integer", "description": "The inclusive line number to end reading at, 1-based. Only provide if the file is too large to read at once."}}}}
</function>
<function>
{"name": "grep_code", "description": "High-performance file content search utilizing the ripgrep engine. Supports full regex, automatic .gitignore honoring, and auto-expansion of matched results to complete syntax blocks (functions, methods, structs) for better context.\n\nGuidelines:\n- Recommended for precise string matching.\n- Full regex support (e.g. \"error.*Handler\", \"class\\\\s+\\\\w+\"). Special characters require escaping for exact matches.\n- Pattern format follows ripgrep conventions.\n- Apply 'type' or 'glob' parameters only when file type is certain.\n- Results auto-expand to complete syntax blocks for better context.\n- Single-line matching by default. Cross-line patterns require multiline: true.", "parameters": {"type": "object", "required": ["regex"], "properties": {"regex": {"type": "string", "description": "Regex pattern for searching within file contents (rg --regexp)."}, "path": {"type": "string", "description": "Target file or directory for search. Uses workspace roots by default."}, "type": {"type": "string", "description": "Specific file type for search (rg --type). Common options: js, py, rust, go, java, etc."}, "glob": {"type": "string", "description": "File filtering glob pattern (rg --glob GLOB), such as \"*.java\" or \"*.{js,ts,tsx}\"."}, "multiline": {"type": "boolean", "description": "Activate multiline matching where . includes newlines and patterns cross line boundaries. Default: false."}, "-A": {"type": "number", "description": "Lines of context to display after match (rg -A)."}, "-B": {"type": "number", "description": "Lines of context to display before match (rg -B)."}, "-C": {"type": "number", "description": "Lines of context to display both before and after match (rg -C)."}, "-i": {"type": "boolean", "description": "Perform case-insensitive search (rg -i). Defaults to false."}}}}
</function>
<function>
{"name": "search_file", "description": "Search for files by glob pattern (such as *.go or config/*.json) in workspace. Only supports glob patterns, NOT regex. Returns the paths of matching files. Limited to 25 results.\n\nExamples:\n- \"*.go\" (converted to \"**/*.go\") - matches all .go files\n- \"**/src/**\" - matches all files under src directories\n- \"**/test/**/test_*.py\" - matches test_*.py files within any test directory", "parameters": {"type": "object", "required": ["query"], "properties": {"query": {"type": "string", "description": "Glob pattern for matching files. If the pattern does not begin with \"**/\", it will be automatically prefixed with \"**/\" for recursive search."}, "path": {"type": "string", "description": "Directory path to search within. Supports both absolute and relative paths."}}}}
</function>
<function>
{"name": "run_in_terminal", "description": "Execute a shell command in the terminal. Use for terminal operations like git, npm, docker, build commands, test runners, etc.\n\nRules:\n- For long-running or persistent processes, set is_background to true.\n- Background terminals return a terminal_id for later output checking via get_terminal_output.\n- Use absolute paths and avoid cd when possible.\n- Do not use newline characters in the command.\n- Commands must not exceed 500 characters.", "parameters": {"type": "object", "required": ["command", "is_background"], "properties": {"command": {"type": "string", "description": "The command to run in the terminal."}, "is_background": {"type": "boolean", "description": "Whether the command starts a background process. If true, the command runs in the background and returns a terminal_id."}, "timeout": {"type": "integer", "description": "Timeout in milliseconds for the command to complete (default 180000, max 1800000)."}, "has_risk": {"type": "boolean", "description": "Whether the command is potentially risky."}, "required_permissions": {"type": "string", "description": "Set to 'all' to request running outside the sandbox."}, "command_names": {"type": "array", "items": {"type": "string"}, "description": "Parsed command names from the command."}}}}
</function>
<function>
{"name": "fetch_content", "description": "Fetches the main content from a web page. Useful for summarizing or analyzing the content of a webpage. The URL must be an HTTP or HTTPS URL pointing to a valid internet resource.", "parameters": {"type": "object", "required": ["url"], "properties": {"url": {"type": "string", "description": "URL to read content from. Must start with http:// or https://."}, "query": {"type": "string", "description": "Optional query to search for in the web page's content. Should be a clear and concise description of the content you want to find."}}}}
</function>
<function>
{"name": "search_web", "description": "Explore the web for real-time information on any topic. Use when you need up-to-date information that might not be in existing knowledge, or when you need to verify current facts. Returns relevant snippets and URLs from web pages.", "parameters": {"type": "object", "required": ["query"], "properties": {"query": {"type": "string", "description": "Search query. Value range: 1 to 100 characters."}, "timeRange": {"type": "string", "enum": ["OneDay", "OneWeek", "OneMonth", "OneYear", "NoLimit"], "description": "The time range of the query. Default is NoLimit."}}}}
</function>
<function>
{"name": "list_dir", "description": "List the contents of a directory. Useful to understand the file structure before diving deeper into specific files. The path parameter must be an absolute path.", "parameters": {"type": "object", "required": ["path"], "properties": {"path": {"type": "string", "description": "Absolute path to list contents of."}}}}
</function>
<function>
{"name": "search_codebase", "description": "Semantic code search that finds code by meaning, not exact text. Use when you need to understand how something works, where functionality is implemented, or explore unfamiliar codebases. Finds relevant code across the entire project based on natural language queries.\n\nExamples: \"authentication logic\", \"how payments work\", \"database connection handling\".", "parameters": {"type": "object", "required": ["query", "key_words"], "properties": {"query": {"type": "string", "description": "A concise, keyword-rich search string optimized for code retrieval. Should contain technical terms, relevant programming concepts, and implementation-focused language."}, "key_words": {"type": "string", "description": "Extract maximum three most important keywords from the query, ranked by relevance. Format: keyword1,keyword2,keyword3"}, "target_directories": {"type": "array", "items": {"type": "string"}, "description": "Absolute directory paths to narrow the search scope."}}}}
</function>
<function>
{"name": "search_symbol", "description": "Tool for discovering code symbols and their relationships. Use for class names, method names, variable names, or any visible symbol names. Supports relationship queries such as calls, called_by, references, referenced_by, extends, extended_by, implements, implemented_by, contains, contained_by, overrides, overridden_by, all, none.", "parameters": {"type": "object", "required": ["queries"], "properties": {"queries": {"type": "array", "items": {"type": "object", "properties": {"symbol": {"type": "string", "description": "The symbol name to search for. One symbol per object."}, "file_path": {"type": "string", "description": "Optional file path to narrow the search."}, "relation": {"type": "string", "enum": ["calls", "called_by", "references", "referenced_by", "extends", "extended_by", "implements", "implemented_by", "contains", "contained_by", "overrides", "overridden_by", "all", "none"], "description": "The relationship to retrieve for this symbol."}}}, "description": "Array of search objects describing symbol lookups."}}}}
</function>
<function>
{"name": "get_terminal_output", "description": "Get the output of a terminal command previously started with run_in_terminal. The tool will intelligently wait for command completion based on the wait_seconds parameter.", "parameters": {"type": "object", "required": ["terminal_id"], "properties": {"terminal_id": {"type": "string", "description": "The ID of the terminal command output to check."}, "wait_seconds": {"type": "integer", "description": "Wait time in seconds for command completion. Choose based on command type: 2s for quick commands, 15s for compilation/tests, 30s for app startup. Default is 2s."}}}}
</function>
<function>
{"name": "create_file", "description": "Create a new file with content or overwrite an existing file. Use this tool to write new files or completely replace existing file contents.\n\nRequirements:\n- file_path must be an absolute path.\n- File content must not exceed 1000 lines.\n- Always prefer editing existing files over creating new ones.\n- Never proactively create documentation files unless explicitly requested.", "parameters": {"type": "object", "required": ["file_path", "file_content"], "properties": {"file_path": {"type": "string", "description": "Absolute path to the target file."}, "file_content": {"type": "string", "description": "The content of the file."}, "add_last_line_newline": {"type": "boolean", "description": "Whether to add a newline at the end of the file. Default: true."}}}}
</function>
<function>
{"name": "delete_file", "description": "Delete a file safely. This is the only safe tool to delete files. Do not use shell commands to remove files.\n\nRequirements:\n- file_path must be an absolute path.", "parameters": {"type": "object", "required": ["file_path"], "properties": {"file_path": {"type": "string", "description": "The absolute file path to delete."}}}}
</function>
<function>
{"name": "search_replace", "description": "Performs exact string replacements in files. Use for editing existing files by replacing specific text with new text.\n\nRules:\n- Preserve exact indentation (tabs/spaces) as it appears in the original.\n- The edit will FAIL if old_string is not unique in the file. Provide more surrounding context to make it unique, or use replace_all.\n- Use replace_all for renaming variables or replacing all occurrences.", "parameters": {"type": "object", "required": ["file_path", "old_string", "new_string"], "properties": {"file_path": {"type": "string", "description": "The path to the file to modify."}, "old_string": {"type": "string", "description": "The text to replace. Must be unique in the file unless replace_all is true."}, "new_string": {"type": "string", "description": "The text to replace it with (must be different from old_string)."}, "replace_all": {"type": "boolean", "description": "Replace all occurrences of old_string. Default: false."}}}}
</function>
<function>
{"name": "get_problems", "description": "Get any compile or lint errors in a code file. If the user mentions errors or problems in a file, they may be referring to these. Use this tool to see the same errors the user is seeing.", "parameters": {"type": "object", "required": ["file_paths"], "properties": {"file_paths": {"type": "array", "items": {"type": "string", "description": "The absolute path of a file to check."}, "description": "The absolute paths of mentioned or edited files."}}}}
</function>
<function>
{"name": "todo_write", "description": "Create and manage a todo list for tracking complex multi-step tasks. Use proactively for complex tasks (3+ steps), non-trivial tasks, user-requested lists, or multiple tasks.\n\nStatus values: PENDING, IN_PROGRESS, COMPLETE, CANCELLED.", "parameters": {"type": "object", "required": ["merge", "todos"], "properties": {"merge": {"type": "boolean", "description": "Whether to merge the todos with existing. true=merge by id, false=replace all."}, "todos": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "description": "Unique identifier for the todo item."}, "content": {"type": "string", "description": "The description/content of the todo item."}, "status": {"type": "string", "enum": ["PENDING", "IN_PROGRESS", "COMPLETE", "CANCELLED"], "description": "The status of the todo item."}}, "required": ["id", "content", "status"]}, "description": "Array of todo items (minimum 2 items recommended)."}}}}
</function>
<function>
{"name": "TaskGet", "description": "Retrieve a task by its ID from the task list. Returns full task details including subject, description, status, blocks, and blockedBy.", "parameters": {"type": "object", "required": ["taskId"], "properties": {"taskId": {"type": "string", "description": "The ID of the task to retrieve."}}}}
</function>
<function>
{"name": "TaskUpdate", "description": "Update a task in the task list. Use to mark tasks as resolved, update details, or establish dependencies.\n\nUpdatable fields: status, subject, description, activeForm, owner, metadata, addBlocks, addBlockedBy.\n\nStatus progresses: pending → in_progress → completed.", "parameters": {"type": "object", "required": ["taskId"], "properties": {"taskId": {"type": "string", "description": "The ID of the task to update."}, "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled", "failed"], "description": "New status for the task."}, "subject": {"type": "string", "description": "New subject for the task (imperative form)."}, "description": {"type": "string", "description": "New description for the task."}, "owner": {"type": "string", "description": "New owner for the task (agent name)."}, "addBlocks": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that this task blocks."}, "addBlockedBy": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that block this task."}, "metadata": {"type": "object", "description": "Metadata keys to merge into the task."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress."}}}}
</function>
<function>
{"name": "SendMessage", "description": "Send a message to another agent in the team. Use to communicate with other agents (leader or experts) — notify about task completion, ask questions, share findings, or request clarification.", "parameters": {"type": "object", "required": ["recipient", "content"], "properties": {"recipient": {"type": "string", "description": "The name of the recipient agent (e.g., 'leader', expert agent name)."}, "content": {"type": "string", "description": "The main content of the message."}, "summary": {"type": "string", "description": "A brief summary of the message (shown in notifications)."}, "type": {"type": "string", "enum": ["message"], "description": "The type of message. Use 'message' for standard messages."}}}}
</function>
<function>
{"name": "update_memory", "description": "Update, add, or delete memories based on the user's intent to maintain memory consistency. Supports creating, updating, and deleting memories across various categories.\n\nMemory categories include: user_info, user_hobby, user_communication, project_tech_stack, project_build_configuration, project_dependency_configuration, project_ide_configuration, project_scm_configuration, project_environment_configuration, development_code_specification, development_practice_specification, development_test_specification, development_comment_specification, expert_experience, learned_skill_experience.", "parameters": {"type": "object", "required": ["title", "content", "keywords", "category", "action"], "properties": {"title": {"type": "string", "description": "Descriptive title for the memory."}, "content": {"type": "string", "description": "Content of the memory."}, "keywords": {"type": "string", "description": "Keywords associated with the memory, comma-separated."}, "category": {"type": "string", "description": "Memory category (e.g., user_info, project_tech_stack, expert_experience, etc.)."}, "action": {"type": "string", "description": "Action type: 'create', 'update', or 'delete'."}, "id": {"type": "string", "description": "ID of an existing memory to update or delete."}, "scope": {"type": "string", "description": "Memory scope: 'global' (cross-project) or 'workspace' (current project only)."}}}}
</function>
<function>
{"name": "search_memory", "description": "Retrieve detailed memories from the memory system. Use to recall relevant information, background context, past experiences, and known solutions.\n\nDepth levels:\n- 'shallow': Smaller scope, fewer but highly relevant results.\n- 'deep': Larger scope, more comprehensive results.", "parameters": {"type": "object", "required": ["query", "keywords", "category", "depth"], "properties": {"query": {"type": "string", "description": "What you want to recall. Keep it semantically clear, complete, and concise."}, "keywords": {"type": "string", "description": "Up to 5 keywords, comma-separated. Each keyword should not exceed 10 characters."}, "category": {"type": "string", "description": "Memory categories to search, comma-separated. Use empty string to search all categories."}, "depth": {"type": "string", "description": "Recall depth: 'shallow' for focused results, 'deep' for comprehensive retrieval."}}}}
</function>
</functions>

---
name: research-expert-tools
agent: research-expert
tool_count: 17
---

<functions>
<function>
{"name": "read_file", "description": "Reads a file from the local filesystem. You can access any file directly by using this tool. Supports reading entire files or specific line ranges. Also supports reading image files (jpeg/jpg, png, webp).\n\nUsage:\n- Provide just the file path to read the entire file.\n- Optionally specify start_line and end_line to read a specific range of lines.\n- For very large files, use line ranges to read in chunks.\n- Can also read image files when called with the appropriate path.", "parameters": {"type": "object", "required": ["file_path"], "properties": {"file_path": {"type": "string", "description": "Absolute path to the file to read."}, "start_line": {"type": "integer", "description": "The line number to start reading from, 1-based. Only provide if the file is too large to read at once."}, "end_line": {"type": "integer", "description": "The inclusive line number to end reading at, 1-based. Only provide if the file is too large to read at once."}}}}
</function>
<function>
{"name": "grep_code", "description": "High-performance file content search utilizing the ripgrep engine. Supports full regex, automatic .gitignore honoring, and auto-expansion of matched results to complete syntax blocks (functions, methods, structs) for better context.\n\nGuidelines:\n- Recommended for precise string matching.\n- Full regex support (e.g. \"error.*Handler\", \"class\\\\s+\\\\w+\"). Special characters require escaping for exact matches.\n- Pattern format follows ripgrep conventions.\n- Apply 'type' or 'glob' parameters only when file type is certain.\n- Results auto-expand to complete syntax blocks for better context.\n- Single-line matching by default. Cross-line patterns require multiline: true.", "parameters": {"type": "object", "required": ["regex"], "properties": {"regex": {"type": "string", "description": "Regex pattern for searching within file contents (rg --regexp)."}, "path": {"type": "string", "description": "Target file or directory for search. Uses workspace roots by default."}, "type": {"type": "string", "description": "Specific file type for search (rg --type). Common options: js, py, rust, go, java, etc."}, "glob": {"type": "string", "description": "File filtering glob pattern (rg --glob GLOB), such as \"*.java\" or \"*.{js,ts,tsx}\"."}, "multiline": {"type": "boolean", "description": "Activate multiline matching where . includes newlines and patterns cross line boundaries. Default: false."}, "-A": {"type": "number", "description": "Lines of context to display after match (rg -A)."}, "-B": {"type": "number", "description": "Lines of context to display before match (rg -B)."}, "-C": {"type": "number", "description": "Lines of context to display both before and after match (rg -C)."}, "-i": {"type": "boolean", "description": "Perform case-insensitive search (rg -i). Defaults to false."}}}}
</function>
<function>
{"name": "search_file", "description": "Search for files by glob pattern (such as *.go or config/*.json) in workspace. Only supports glob patterns, NOT regex. Returns the paths of matching files. Limited to 25 results.\n\nExamples:\n- \"*.go\" (converted to \"**/*.go\") - matches all .go files\n- \"**/src/**\" - matches all files under src directories\n- \"**/test/**/test_*.py\" - matches test_*.py files within any test directory", "parameters": {"type": "object", "required": ["query"], "properties": {"query": {"type": "string", "description": "Glob pattern for matching files. If the pattern does not begin with \"**/\", it will be automatically prefixed with \"**/\" for recursive search."}, "path": {"type": "string", "description": "Directory path to search within. Supports both absolute and relative paths."}}}}
</function>
<function>
{"name": "run_in_terminal", "description": "Execute a shell command in the terminal. Use for terminal operations like git, npm, docker, build commands, test runners, etc.\n\nRules:\n- For long-running or persistent processes, set is_background to true.\n- Background terminals return a terminal_id for later output checking via get_terminal_output.\n- Use absolute paths and avoid cd when possible.\n- Do not use newline characters in the command.\n- Commands must not exceed 500 characters.", "parameters": {"type": "object", "required": ["command", "is_background"], "properties": {"command": {"type": "string", "description": "The command to run in the terminal."}, "is_background": {"type": "boolean", "description": "Whether the command starts a background process. If true, the command runs in the background and returns a terminal_id."}, "timeout": {"type": "integer", "description": "Timeout in milliseconds for the command to complete (default 180000, max 1800000)."}, "has_risk": {"type": "boolean", "description": "Whether the command is potentially risky."}, "required_permissions": {"type": "string", "description": "Set to 'all' to request running outside the sandbox."}, "command_names": {"type": "array", "items": {"type": "string"}, "description": "Parsed command names from the command."}}}}
</function>
<function>
{"name": "fetch_content", "description": "Fetches the main content from a web page. Useful for summarizing or analyzing the content of a webpage. The URL must be an HTTP or HTTPS URL pointing to a valid internet resource.", "parameters": {"type": "object", "required": ["url"], "properties": {"url": {"type": "string", "description": "URL to read content from. Must start with http:// or https://."}, "query": {"type": "string", "description": "Optional query to search for in the web page's content. Should be a clear and concise description of the content you want to find."}}}}
</function>
<function>
{"name": "search_web", "description": "Explore the web for real-time information on any topic. Use when you need up-to-date information that might not be in existing knowledge, or when you need to verify current facts. Returns relevant snippets and URLs from web pages.", "parameters": {"type": "object", "required": ["query"], "properties": {"query": {"type": "string", "description": "Search query. Value range: 1 to 100 characters."}, "timeRange": {"type": "string", "enum": ["OneDay", "OneWeek", "OneMonth", "OneYear", "NoLimit"], "description": "The time range of the query. Default is NoLimit."}}}}
</function>
<function>
{"name": "list_dir", "description": "List the contents of a directory. Useful to understand the file structure before diving deeper into specific files. The path parameter must be an absolute path.", "parameters": {"type": "object", "required": ["path"], "properties": {"path": {"type": "string", "description": "Absolute path to list contents of."}}}}
</function>
<function>
{"name": "search_codebase", "description": "Semantic code search that finds code by meaning, not exact text. Use when you need to understand how something works, where functionality is implemented, or explore unfamiliar codebases. Finds relevant code across the entire project based on natural language queries.\n\nExamples: \"authentication logic\", \"how payments work\", \"database connection handling\".", "parameters": {"type": "object", "required": ["query", "key_words"], "properties": {"query": {"type": "string", "description": "A concise, keyword-rich search string optimized for code retrieval. Should contain technical terms, relevant programming concepts, and implementation-focused language."}, "key_words": {"type": "string", "description": "Extract maximum three most important keywords from the query, ranked by relevance. Format: keyword1,keyword2,keyword3"}, "target_directories": {"type": "array", "items": {"type": "string"}, "description": "Absolute directory paths to narrow the search scope."}}}}
</function>
<function>
{"name": "search_symbol", "description": "Tool for discovering code symbols and their relationships. Use for class names, method names, variable names, or any visible symbol names. Supports relationship queries such as calls, called_by, references, referenced_by, extends, extended_by, implements, implemented_by, contains, contained_by, overrides, overridden_by, all, none.", "parameters": {"type": "object", "required": ["queries"], "properties": {"queries": {"type": "array", "items": {"type": "object", "properties": {"symbol": {"type": "string", "description": "The symbol name to search for. One symbol per object."}, "file_path": {"type": "string", "description": "Optional file path to narrow the search."}, "relation": {"type": "string", "enum": ["calls", "called_by", "references", "referenced_by", "extends", "extended_by", "implements", "implemented_by", "contains", "contained_by", "overrides", "overridden_by", "all", "none"], "description": "The relationship to retrieve for this symbol."}}}, "description": "Array of search objects describing symbol lookups."}}}}
</function>
<function>
{"name": "get_terminal_output", "description": "Get the output of a terminal command previously started with run_in_terminal. The tool will intelligently wait for command completion based on the wait_seconds parameter.", "parameters": {"type": "object", "required": ["terminal_id"], "properties": {"terminal_id": {"type": "string", "description": "The ID of the terminal command output to check."}, "wait_seconds": {"type": "integer", "description": "Wait time in seconds for command completion. Choose based on command type: 2s for quick commands, 15s for compilation/tests, 30s for app startup. Default is 2s."}}}}
</function>
<function>
{"name": "get_problems", "description": "Get any compile or lint errors in a code file. If the user mentions errors or problems in a file, they may be referring to these. Use this tool to see the same errors the user is seeing.", "parameters": {"type": "object", "required": ["file_paths"], "properties": {"file_paths": {"type": "array", "items": {"type": "string", "description": "The absolute path of a file to check."}, "description": "The absolute paths of mentioned or edited files."}}}}
</function>
<function>
{"name": "todo_write", "description": "Create and manage a todo list for tracking complex multi-step tasks. Use proactively for complex tasks (3+ steps), non-trivial tasks, user-requested lists, or multiple tasks.\n\nStatus values: PENDING, IN_PROGRESS, COMPLETE, CANCELLED.", "parameters": {"type": "object", "required": ["merge", "todos"], "properties": {"merge": {"type": "boolean", "description": "Whether to merge the todos with existing. true=merge by id, false=replace all."}, "todos": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "description": "Unique identifier for the todo item."}, "content": {"type": "string", "description": "The description/content of the todo item."}, "status": {"type": "string", "enum": ["PENDING", "IN_PROGRESS", "COMPLETE", "CANCELLED"], "description": "The status of the todo item."}}, "required": ["id", "content", "status"]}, "description": "Array of todo items (minimum 2 items recommended)."}}}}
</function>
<function>
{"name": "TaskGet", "description": "Retrieve a task by its ID from the task list. Returns full task details including subject, description, status, blocks, and blockedBy.", "parameters": {"type": "object", "required": ["taskId"], "properties": {"taskId": {"type": "string", "description": "The ID of the task to retrieve."}}}}
</function>
<function>
{"name": "TaskUpdate", "description": "Update a task in the task list. Use to mark tasks as resolved, update details, or establish dependencies.\n\nUpdatable fields: status, subject, description, activeForm, owner, metadata, addBlocks, addBlockedBy.\n\nStatus progresses: pending → in_progress → completed.", "parameters": {"type": "object", "required": ["taskId"], "properties": {"taskId": {"type": "string", "description": "The ID of the task to update."}, "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled", "failed"], "description": "New status for the task."}, "subject": {"type": "string", "description": "New subject for the task (imperative form)."}, "description": {"type": "string", "description": "New description for the task."}, "owner": {"type": "string", "description": "New owner for the task (agent name)."}, "addBlocks": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that this task blocks."}, "addBlockedBy": {"type": "array", "items": {"type": "string"}, "description": "Task IDs that block this task."}, "metadata": {"type": "object", "description": "Metadata keys to merge into the task."}, "activeForm": {"type": "string", "description": "Present continuous form shown in spinner when in_progress."}}}}
</function>
<function>
{"name": "SendMessage", "description": "Send a message to another agent in the team. Use to communicate with other agents (leader or experts) — notify about task completion, ask questions, share findings, or request clarification.", "parameters": {"type": "object", "required": ["recipient", "content"], "properties": {"recipient": {"type": "string", "description": "The name of the recipient agent (e.g., 'leader', expert agent name)."}, "content": {"type": "string", "description": "The main content of the message."}, "summary": {"type": "string", "description": "A brief summary of the message (shown in notifications)."}, "type": {"type": "string", "enum": ["message"], "description": "The type of message. Use 'message' for standard messages."}}}}
</function>
<function>
{"name": "update_memory", "description": "Update, add, or delete memories based on the user's intent to maintain memory consistency. Supports creating, updating, and deleting memories across various categories.\n\nMemory categories include: user_info, user_hobby, user_communication, project_tech_stack, project_build_configuration, project_dependency_configuration, project_ide_configuration, project_scm_configuration, project_environment_configuration, development_code_specification, development_practice_specification, development_test_specification, development_comment_specification, expert_experience, learned_skill_experience.", "parameters": {"type": "object", "required": ["title", "content", "keywords", "category", "action"], "properties": {"title": {"type": "string", "description": "Descriptive title for the memory."}, "content": {"type": "string", "description": "Content of the memory."}, "keywords": {"type": "string", "description": "Keywords associated with the memory, comma-separated."}, "category": {"type": "string", "description": "Memory category (e.g., user_info, project_tech_stack, expert_experience, etc.)."}, "action": {"type": "string", "description": "Action type: 'create', 'update', or 'delete'."}, "id": {"type": "string", "description": "ID of an existing memory to update or delete."}, "scope": {"type": "string", "description": "Memory scope: 'global' (cross-project) or 'workspace' (current project only)."}}}}
</function>
<function>
{"name": "search_memory", "description": "Retrieve detailed memories from the memory system. Use to recall relevant information, background context, past experiences, and known solutions.\n\nDepth levels:\n- 'shallow': Smaller scope, fewer but highly relevant results.\n- 'deep': Larger scope, more comprehensive results.", "parameters": {"type": "object", "required": ["query", "keywords", "category", "depth"], "properties": {"query": {"type": "string", "description": "What you want to recall. Keep it semantically clear, complete, and concise."}, "keywords": {"type": "string", "description": "Up to 5 keywords, comma-separated. Each keyword should not exceed 10 characters."}, "category": {"type": "string", "description": "Memory categories to search, comma-separated. Use empty string to search all categories."}, "depth": {"type": "string", "description": "Recall depth: 'shallow' for focused results, 'deep' for comprehensive retrieval."}}}}
</function>
</functions>

